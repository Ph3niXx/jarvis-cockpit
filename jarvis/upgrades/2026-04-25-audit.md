# Audit Jarvis — 2026-04-25

## North Star de la semaine

Cette semaine, le diagnostic hebdo de Jarvis doit redevenir fiable — `extract_signals.py` doit refléter le vrai cockpit, et un échec LM Studio mid-run ne doit plus brûler 55 minutes en silence.

## Ce qui a bougé depuis le dernier audit

- **165 commits depuis le 18/04**, dont la migration React quasi-complète, le panel Jarvis Lab + section Architecture (4 vues), 25 specs `docs/specs/tab-*.md`, 4 fichiers `docs/architecture/*.yaml`, et 4 nouveaux workflows CI (`spec-drift-check`, `lint-specs`, `validate-arch`, `arch-drift-check`).
- **Shipped depuis 2026-04-23** :
  - v12-S1 — `1e26898 fix(jarvis): dedicated non-thinking model for nightly extraction` → boucle d'apprentissage rétablie. `extraction_rate=3.8` ce matin (vs `0.0` les 6 jours précédents). 6 faits + 13 entités extraits sur le run du 25/04 à 08:56.
  - v12-K1 — `36113b6 chore(jarvis): workflow hygiene` → 3 orphelins racine supprimés, 4 stale prompts déplacés vers `done/`, INDEX réconcilié.
  - Phase 7 méta-doc complète : règles éditoriales Fonctionnalités + Parcours utilisateur, lints CI bloquants, structure `docs/architecture/`, viewer specs côté Jarvis Lab.
- **Friction résiduelle** :
  - Le 24/04 entre 16:55 et 17:32, **3 timeouts LM Studio consécutifs (~55 min cumulées)** sur les modèles `qwen3-4b-2507` puis `qwen3-4b-thinking-2507`. Échec silencieux : aucun déclenchement Cowork ni de signal dans `/health`. Le préflight v12-S1 a passé — le modèle s'est déchargé *après*, en cours de run.
  - Le rapport `signals.md` du 25/04 n'a pas été généré : la tâche planifiée Python prévue à 05h30 n'a pas tourné (ou elle a tourné sans pousser le fichier dans `intel/`). Audit du jour fait à l'aveugle sur les 8 derniers jours.

## Signaux faibles (télémétrie + logs)

- **Top events 7j (signals 23/04)** : `section_opened` (496), `error_shown` (154), `pipeline_triggered` (25), `link_clicked` (8), `profile_field_saved` (7).
- **Sections jamais ouvertes** : 4/26 listées dans `KNOWN_SECTIONS` (`agents`, `energy`, `papers`, `tools`) — **mais ces 4 sections n'existent plus dans le cockpit React** : faux positifs. À l'inverse, `extract_signals.py` a flaggé **15 sections "inconnues"** (`anime`, `gaming`, `gaming_news`, `jobs`, `music`, `news`, `opps`, `perf`, `sport`, `stacks`, `top`, `veille`, `week`, `gaming-overview`, `music-overview`, `perf-overview`) — ce sont les *vraies* sections du cockpit actuel. La liste de référence est obsolète, l'audit voit l'envers du miroir.
- **Erreurs UI captées** :
  - `panel:wiki` : 108 occurrences (`Cannot read properties of undefined (reading 'toLocaleString')`) — fix CSS commit `209b12a` sans correctif JS. Reste à vérifier sur run actuel.
  - `panel:opps` : 24 occurrences (`Cannot read properties of null (reading 'urgency')`).
  - `panel:history` : 9 occurrences (`Cannot read properties of undefined (reading 'toLocaleString')`).
- **Erreurs pipelines** :
  - `nightly_learner.log` (legacy) garde 6 lignes WARNING du 12-14/04 — déjà fixées par v12-S1 mais toujours remontées par `extract_signals.py` faute de troncature.
  - `last_indexer.log` : 3 erreurs 409 résiduelles (PARK P21 — sous le seuil de 5/run, pas d'action).
  - `cloudflared.log` : 3 erreurs `context canceled` lors d'un teardown du 23/04 — RAS, comportement attendu à l'arrêt du tunnel.
- **Métriques santé** : 752 vecteurs (+45 vs 23/04), 81 faits actifs, 843 events télémétrie totaux, 0 patterns validés/rejetés (pas de couche de validation).
- **`status_generator.log`** : ligne `Chunks indexes: 0` alors que la base a 752 vecteurs → la métrique compte mal (probablement les chunks *du mois en cours* avec une heuristique cassée). Bruit dans le snapshot Supabase, mineur.

## SHIP (max 3, après rechallenging)

### S1 — Resync `KNOWN_SECTIONS` dans `extract_signals.py` sur le vrai cockpit React

**Pourquoi maintenant** : sert directement la North Star. La liste actuelle pollue chaque audit avec 15 fausses anomalies "section inconnue" et 4 fausses sections mortes — le diagnostic est devenu illisible.
**Diagnostic** : `jarvis/scripts/extract_signals.py:31-37` (commentaire `Derniere mise a jour : 2026-04-12`, antérieur à la migration React et à l'ajout de `top`/`review`/`jarvis-lab`/`jobs`/`week`/`stacks`/etc.).
**Définition de "fait"** : après run, `signals.md` du jour ne contient AUCUNE ligne `❓ Section inconnue` et la métrique "Sections jamais ouvertes" descend à ≤ 2 (les vraies sections que je n'ouvre jamais — pas les sections fantômes).
**Effort** : XS (~30 min)

#### Prompt Claude Code — S1

```
Contexte projet : extract_signals.py compare les events `section_opened` à une liste 
hardcodée KNOWN_SECTIONS qui date du 12/04 — antérieure à la migration React + l'ajout 
des panels top/review/jarvis-lab/jobs/week/stacks/perf/music/gaming/anime/news/sport. 
Conséquence : chaque audit hebdo flagge 15 fausses "Section inconnue" et 4 fausses 
"Sections jamais ouvertes" (myweek/llm/agents/energy/finserv/tools/biz/reg/papers/jarvis-project/tft/costs/rte 
n'existent plus dans le cockpit). La liste de référence pointe vers l'envers du miroir.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, compile ce bilan en ~30 lignes :

1. Lis cockpit/app.jsx L425-475 (la chaîne de `else if (activePanel === "...")` est la 
   source de vérité des panels visibles).
2. grep -oE 'activePanel\s*===\s*"[a-z_-]+"' cockpit/app.jsx | sort -u 
   (extrait la liste effective).
3. Vérifie aussi cockpit/sidebar.jsx (s'il existe) pour les data-panel ou navigate 
   appelés.
4. Lis jarvis/scripts/extract_signals.py L28-50 (commentaire actuel + KNOWN_SECTIONS).
5. cat jarvis/intel/2026-04-23-signals.md → relève la liste exacte des "Section inconnue" 
   pour cross-check : tu dois les retrouver toutes dans le cockpit React, sinon ce 
   sont des navigate orphelins à investiguer (pas à ajouter aveuglément).
6. Identifie 4 cas particuliers à arbitrer EXPLICITEMENT (et pose-les à Jean si doute) :
   - "claude" est-il un panel ou une route fallback Tier 2 ? (vu en app.jsx:451)
   - "gaming_news" vs "gaming-news" : underscore ou tiret ? Choix : ce que track() 
     envoie réellement, pas ce qui te paraît joli.
   - "veille" : c'est un alias mort (corpus VEILLE_DATA) ou une vraie nav ?
   - "perf-history" / "music-overview" / "gaming-overview" : sous-vues internes 
     d'un panel composite ou vraie navigation ? Si interne, NE PAS les ajouter à 
     KNOWN_SECTIONS — elles ne devraient pas remonter dans `section_opened`.

Écris un rapport ~25 lignes listant :
- La liste finale proposée (alphabétique, ~25-27 entrées)
- Les sections retirées de l'ancienne liste (avec raison : "supprimée du cockpit" / 
  "renommée en X")
- Les arbitrages des 4 cas particuliers
- La note de maintenance proposée pour le commentaire au-dessus

ATTENDS validation explicite. Ne touche pas au fichier.

Objectif : Faire remonter le bruit "Section inconnue" à 0 dans le prochain run audit, 
et faire remonter "Sections jamais ouvertes" à des sections réellement existantes mais 
peu utilisées.

Fichiers concernés :
- jarvis/scripts/extract_signals.py (modification — UNIQUEMENT le bloc KNOWN_SECTIONS 
  L31-37 + le commentaire au-dessus)

Étapes (après validation) :

1. Remplacer KNOWN_SECTIONS par la liste validée en Phase 0.
2. Mettre à jour le commentaire :
   "# Source de vérité : cockpit/app.jsx, chaîne `else if (activePanel === "...")`.
    # A re-synchroniser quand un panel est ajouté ou retiré.
    # Derniere mise a jour : 2026-04-25 (post-migration React + jarvis-lab)."
3. Lancer `python jarvis/scripts/extract_signals.py` localement et OBSERVER le rapport 
   généré dans jarvis/intel/2026-04-25-signals.md.
4. Vérifier que la section "Anomalies detectees" ne contient plus de ligne 
   "❓ Section inconnue" et que "Sections jamais ouvertes" affiche ≤ 2 sections.

Contraintes :
- Ne touche RIEN d'autre dans extract_signals.py (pas de refactor, pas de seuils changés)
- Respecte l'ordre alphabétique et le format set Python existant
- Ne génère PAS automatiquement la liste depuis app.jsx — la liste manuelle avec note 
  de maintenance est un compromis acceptable
- Ne touche pas à index.html / cockpit/ — ce SHIP est purement extraction-side
- Pas de secret nouveau

Validation :
- python jarvis/scripts/extract_signals.py → exit 0
- grep -c "❓ Section inconnue" jarvis/intel/2026-04-25-signals.md → 0
- grep "Sections jamais ouvertes" jarvis/intel/2026-04-25-signals.md → 
  pourcentage < 15% ET liste sans agents/energy/papers/tools/myweek/llm/finserv/biz/reg

Ne fais PAS :
- Ne génère pas KNOWN_SECTIONS depuis un parsing AST de app.jsx — over-engineering
- N'ajoute pas de subprocess à app.jsx au runtime — couplage inutile
- Ne refactore pas extract_signals.py au-delà du bloc KNOWN_SECTIONS
- Ne touche pas aux 8 règles d'anomalies (Phase 1 du Weekly Pipeline est explicite : 
  ajuster les seuils est un autre sujet)
- Ne push pas

Quand c'est fait : montre-moi le diff complet AVANT git add + le contenu de la section 
Anomalies de signals.md du jour. 
git commit avec message "fix(audit): resync KNOWN_SECTIONS on real React cockpit panels".
PAS de push.
```

#### Rechallenging — S1
- Red team : "Pourquoi tu ne génères pas la liste à la volée depuis `app.jsx` ? Tu vas redériver dans 1 mois." Objection valide — mais le commentaire de maintenance + la cible (1 modif/mois) gardent le coût marginal sous celui d'un parser AST jsx en Python. Compromis assumé.
- Contre-exemple : si je supprime `myweek` mais qu'un futur `navigate("myweek")` orphelin reste dans le code, je perds le signal "panel mort". Faible : le test inverse (15 sections "inconnues") pour les nouvelles sections couvre déjà ça côté lecture.
- Coût d'opportunité : 30 min vs (a) ouvrir le PoC Mem0g (PARK, M effort, hors scope), (b) re-creuser P25 audit panel-by-panel (M, attend incident). S1 reste meilleur usage.
- Futur soi : "J'aurais dû voir que la liste allait redériver de toute façon." Mitigé par le commentaire — mais effet limité, à accepter.
- Cohérence historique : RAS, aucune ligne INDEX ne touche `KNOWN_SECTIONS`.
- **Verdict final : MAINTENU**

### S2 — Détection LM Studio "model unloaded" mid-run dans `_sync_call` (abort propre + state degraded)

**Pourquoi maintenant** : sert directement la North Star. Le 24/04 entre 16:55 et 17:32, **3 timeouts × 1108s ont consommé 55 minutes en silence** (`llm_traces.jsonl`). Le préflight existant ne couvre que le démarrage — si le modèle se décharge après, on retry 3 fois × 18 minutes. Aucune visibilité, aucune alerte.
**Diagnostic** : `jarvis/llm_client.py:91-97` (boucle de retry sur `APITimeoutError` sans check d'état entre tentatives). Cf `jarvis_data/llm_traces.jsonl` lignes du 24/04T16:55, 17:13, 17:32 — toutes en `latency_ms ≈ 1108000, attempts: 3, error_type: APITimeoutError`.
**Définition de "fait"** : après modification, un nouveau run avec LM Studio model déchargé mid-run produit un trace `status: "error", error_type: "model_unloaded_midrun"` en moins de **15 secondes** au lieu de 1108s, et `/health` renvoie `lm_studio: "no_model_loaded"` + `status: "degraded"` dans la foulée. Vérifié sur 1 run réel ou simulé.
**Effort** : S (1.5-2h)

#### Prompt Claude Code — S2

```
Contexte projet : Le 24/04, 3 timeouts consécutifs LM Studio (1108s × 3 = 55 min) ont 
brûlé en silence. Cause : LM Studio a déchargé le modèle après le préflight, et la 
boucle retry dans llm_client._sync_call attend 120s × 3 sans vérifier l'état du serveur 
entre tentatives. Le check_lm_studio() existant (GET /v1/models, ~50ms) permet de 
détecter "no_model_loaded" en quasi-temps-réel — il faut juste le câbler dans la boucle 
de retry.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant toute modification :

1. Lis jarvis/llm_client.py EN ENTIER (117 lignes — fais-le).
2. Lis jarvis/server.py L116-205 (l'endpoint /health et la branche `lm_studio`).
3. Lis jarvis/nightly_learner.py L360-400 (préflight existant — référence pour le 
   pattern, mais NE PAS le dupliquer).
4. tail -20 jarvis_data/llm_traces.jsonl (regarde les 3 timeouts du 24/04 — tu dois 
   pouvoir reconstituer le scénario : ping OK puis 3 timeouts × 1108s sur extraction).
5. Vérifie qu'aucun appel à _sync_call ne dépend du comportement "retry après 1108s" 
   (chat /chat, nightly, daily_brief, status_generator, evaluate-challenge, indexer). 
   grep -rn "chat_completion_sync\|chat_completion_async" jarvis/ | head.
6. Identifie où ranger la nouvelle constante de classe d'erreur :
   - "model_unloaded_midrun" comme valeur de error_type dans le trace JSONL
   - faut-il aussi exposer un état dans /health ? Oui, déjà fait via `lm_studio` 
     champ ; pas de nouvelle clé.

Écris un rapport ~30 lignes listant :
- Le diagramme actuel : APITimeoutError → backoff 2s → retry → backoff 4s → retry → raise
- Le diagramme cible : APITimeoutError → check_lm_studio() rapide → si "no_model_loaded" 
  → trace status="error" error_type="model_unloaded_midrun" → raise immédiatement 
  (pas de retry) ; sinon → comportement actuel (backoff + retry).
- Le ou les call sites qui pourraient régresser (lister explicitement, pas vague).
- Si on doit aussi mettre à jour state.last_result côté nightly (suggéré : oui, 
  ajouter une branche "model_unloaded_midrun" dans nightly_learner._llm_extract pour 
  setter state["last_result"] = "lm_studio_unloaded_midrun" si on récupère cette 
  classe d'erreur — distinct de "extraction_model_unavailable" qui est une absence 
  au démarrage).

ATTENDS validation explicite avant de coder.

Objectif : Sur un run où LM Studio décharge le modèle mid-run, abort en < 15s avec 
trace lisible et état /health degraded — au lieu de brûler 55 minutes en silence.

Fichiers concernés :
- jarvis/llm_client.py (modification ciblée du except APITimeoutError dans _sync_call)
- jarvis/nightly_learner.py (ajout d'une branche de capture pour l'error_type 
  "model_unloaded_midrun", set state["last_result"], early return)
- jarvis/server.py (déjà câblé via /health → vérifier que `last_result == 
  "lm_studio_unloaded_midrun"` déclenche `status: "degraded"` ; sinon ajouter à la 
  liste des marqueurs degraded L156)

Étapes (après validation) :

1. Dans jarvis/llm_client.py, dans la boucle for de _sync_call :
   ```python
   except (APITimeoutError, APIConnectionError) as e:
       last_exc = e
       # Mid-run model unload detection — check before retry
       if isinstance(e, APITimeoutError):
           lm_state = check_lm_studio(timeout=2.0)
           if lm_state == "no_model_loaded":
               _write_trace({
                   "ts": datetime.now(timezone.utc).isoformat(),
                   "model": model_used,
                   "chars_in": chars_in,
                   "latency_ms": round((time.time() - start_time) * 1000),
                   "attempts": attempt + 1,
                   "status": "error",
                   "error_type": "model_unloaded_midrun",
               })
               raise RuntimeError(
                   f"LM Studio unloaded model {model_used} mid-run (after attempt {attempt+1})"
               ) from e
       if attempt < MAX_RETRIES:
           backoff = 2 ** (attempt + 1)
           log.warning("LLM retry %d/%d after %s (backoff %ds)", attempt + 1, MAX_RETRIES, type(e).__name__, backoff)
           time.sleep(backoff)
   ```
   Le check_lm_studio() ajoute ~50ms à chaque APITimeoutError — coût trivial.

2. Dans jarvis/nightly_learner.py::_llm_extract (~L160-180) :
   - Wrapper l'appel chat_completion_sync dans un try/except `RuntimeError` qui 
     contient "unloaded model"
   - Si capté : log.error + state["last_result"] = "lm_studio_unloaded_midrun" + 
     re-raise (laisser run() voir l'arrêt — tu choisis : early return propre dans 
     run(), ou propagation jusqu'au catch global)
   - Cohérent avec la branche existante "extraction_model_unavailable"

3. Dans jarvis/server.py L156 :
   - Ajouter "lm_studio_unloaded_midrun" à la liste des last_result qui passent 
     /health en `status: "degraded"`.

4. Test reproductible (sans casser la prod) :
   ```bash
   # Avec LM Studio démarré + modèle chargé :
   python -c "from jarvis.llm_client import chat_completion_sync; \
     out, t = chat_completion_sync([{'role':'user','content':'ping'}], max_tokens=5); \
     print('OK', out, t)"
   # → doit fonctionner normalement
   
   # Décharger manuellement le modèle dans LM Studio (UI), puis :
   python -c "from jarvis.llm_client import chat_completion_sync; \
     out, t = chat_completion_sync([{'role':'user','content':'ping'}], max_tokens=5); \
     print('OK', out, t)"
   # → doit lever RuntimeError 'unloaded model' en < 15s
   ```

Contraintes :
- Ne supprime PAS les retries existants — APIConnectionError reste retry-friendly 
  (LM Studio peut ne pas répondre brièvement sans avoir déchargé)
- Ne touche pas au préflight existant dans nightly_learner — il complète ce SHIP, pas 
  l'inverse
- Ne touche pas à l'orchestrateur _route_llm dans server.py (HTTPException 504 
  reste correct pour /chat — le RuntimeError remonte naturellement à 500)
- Ne change PAS la signature publique de chat_completion_sync / chat_completion_async
- Pas de nouvelle dépendance, pas de variable d'env nouvelle
- check_lm_studio doit timeout court (2s max) — sinon on rajoute du temps mort

Validation :
- python jarvis/scripts/smoke_test_chat.py 2>/dev/null OR test manuel : 1 ping OK 
  puis 1 ping après unload manuel → exception en < 15s, trace error_type 
  "model_unloaded_midrun" présente
- grep -c "model_unloaded_midrun" jarvis_data/llm_traces.jsonl → ≥ 1 après le test
- curl http://localhost:8765/health (modèle déchargé) → "lm_studio": "no_model_loaded", 
  "status": "degraded"
- python -m py_compile jarvis/llm_client.py jarvis/nightly_learner.py jarvis/server.py → OK
- Les autres call sites (status_generator, daily_brief_generator, evaluate-challenge) 
  doivent compiler aussi : python -m py_compile $(grep -lE "chat_completion" jarvis/*.py)

Ne fais PAS :
- N'ajoute pas un mécanisme de re-load automatique du modèle (over-engineering, 
  appartient à start_jarvis.bat)
- N'ajoute pas de circuit breaker stateful (pour 1 cas en 7 jours, pas de valeur)
- N'ajoute pas de notification email/Slack/push (hors scope)
- Ne refactore pas _sync_call au-delà du bloc except cité
- Ne push pas
- Ne touche pas au cockpit (panel-jarvis.jsx etc.)

Quand c'est fait : montre-moi le diff + la trace JSONL d'un test réel (1 ping OK + 1 
ping après unload). git commit avec message "feat(jarvis): detect LM Studio model 
unload mid-run, abort fast with state.degraded". 
PAS de push.
```

#### Rechallenging — S2
- Red team : "Le préflight v12-S1 et le S1-v10 (pending) couvraient déjà l'unavailability LM Studio. Pourquoi un 3e angle ?" Distinction : v12-S1 = vérifie le modèle d'extraction au démarrage (couvre absent), v10-S1 (pending) = préflight général au démarrage (idem). Aucun des deux ne détecte un *unload mid-run* — c'est précisément le scénario du 24/04. Couverture orthogonale, pas redondante.
- Contre-exemple : si LM Studio renvoie un timeout réseau réel (pas un unload), on ferait un faux positif "model_unloaded_midrun". Réfutation : `check_lm_studio()` interroge `GET /v1/models` (50ms) — si le serveur est joignable mais sans modèle chargé, la distinction est précise. Si le serveur est down, on retombe sur `unreachable` et on retry comme avant.
- Coût d'opportunité : 1.5-2h vs (a) Mem0g PoC (M, hors scope), (b) bumper le project_status.yaml (XS — rétrogradé, voir SHIP rétrogradés). S2 ciblé North Star.
- Futur soi : "J'ai sur-investi sur un cas qui ne se reproduira jamais." Probabilité réelle : LM Studio unload est documenté côté communauté (économie VRAM, low-power mode après 5 min d'inactivité). C'est arrivé une fois cette semaine — pari raisonnable que ça arrive ≥ 1x/semaine pendant les sessions chat espacées.
- Cohérence historique : v10-S1 (préflight LM Studio) reste `proposed` dans l'INDEX. Ce SHIP ne le contredit pas, il étend. À noter dans la mise à jour INDEX.
- **Verdict final : MAINTENU**

## SHIP rétrogradés pendant le rechallenging

**S3 (tentatif) — Bump `project_status.yaml` avec Phase 7 méta-doc** → rétrogradé en **PARK P27**.
- Rétrogradation déclenchée par : red team forte ("aucun gain fonctionnel, pure cosmétique"), futur soi ("pourquoi j'ai bumpé un YAML alors que 1000 priorités plus urgentes ?").
- Condition de réveil : quand l'ensemble Phase 7 (specs + architecture YAML + 4 lints CI) tourne 7 jours sans warning ni intervention manuelle → bumper en bloc avec une vraie phase "Phase 7 — Méta-documentation et garde-fous CI".

## KILL list

### K1 — Supprimer `jarvis_data/nightly_learner.log` (legacy 12-14/04, périmé) + classer le pending stale `2026-04-14-S1-preflight-lm-studio.md`

**Raison** : Ce log cumulatif contient 50 lignes datées des 12-14/04, dont 6 WARNING déjà fixés par v12-S1 (`response_format` + `request timed out`). À chaque audit, `extract_signals.py` les remonte dans "Erreurs pipelines". Bruit permanent, valeur diagnostic = 0. `last_nightly_learner.log` (régénéré à chaque run) couvre le besoin de debug actif. Pendant qu'on y est, le pending `2026-04-14-S1-preflight-lm-studio.md` traîne depuis 11 jours — il sera partiellement absorbé par v13-S2 (qui couvre le scénario mid-run, plus large que le préflight initial). On le déplace en `done/` avec note d'absorption.
**Risque de regret** : Faible. Les 6 erreurs périmées sont publiques dans l'INDEX et l'audit du 23/04. Aucune perte de mémoire institutionnelle.
**Rechallenging court** :
- Test 1 (red team) : "Tu supprimes un log qui pourrait servir à reproduire un bug." Réfutation : le bug est connu, fixé, et non-reproductible (on a changé de modèle). Périmé.
- Test 4 (futur soi) : "J'ai cassé une enquête future." Faible — `git log` + commit `1e26898` portent l'historique du fix.

**Prompt Claude Code** :

```
Phase 0 — Vérification :
1. wc -l jarvis_data/nightly_learner.log (doit faire ~50 lignes, max 100)
2. tail -3 jarvis_data/nightly_learner.log (doit être daté ≤ 2026-04-14)
3. ls jarvis_data/last_nightly_learner.log (doit exister — c'est le successeur live)
4. ls jarvis/upgrades/prompts/pending/2026-04-14-S1-preflight-lm-studio.md
5. ls jarvis/upgrades/prompts/done/2026-04-14-S1-preflight-lm-studio.md (vérifier 
   qu'il n'y a PAS déjà un fichier homonyme dans done/ — sinon renommer en -v2)

Si OK :
6. rm jarvis_data/nightly_learner.log
7. git mv jarvis/upgrades/prompts/pending/2026-04-14-S1-preflight-lm-studio.md \
        jarvis/upgrades/prompts/done/2026-04-14-S1-preflight-lm-studio.md
8. (si jarvis_data/ est versionné) git rm jarvis_data/nightly_learner.log ; sinon 
   simple rm — vérifier .gitignore : `cat .gitignore | grep jarvis_data`

Note : ce repo gitignore typically `jarvis_data/` (privacy-first). Confirme avec git 
status.

git commit avec message "chore(jarvis): kill stale nightly_learner.log + close stale 
preflight pending (absorbed by v13-S2)".
PAS de push.
```

## PARK

| Idée | Condition de réveil |
|------|---------------------|
| P27 — Bump `project_status.yaml` avec Phase 7 méta-doc | Quand les 4 lints CI Phase 7 tournent 7j sans warning ni intervention manuelle |
| P28 — Hybrid search BM25 + vector + RRF (réveil P2 enrichi par veille 25/04) | Maintenant que vectors=752 (≥500) ET 1 SHIP M de capacité disponible la même semaine (cette semaine c'est S1+S2) — donc la prochaine semaine si aucun incident urgent |
| P29 — Mem0g graph extension (relations + conflict detector + hybrid retrieval) | Quand P28 est shippé ET que un cas d'usage concret de contradiction de faits est observé dans `profile_facts` (au moins 3 cas en 1 semaine) |
| P30 — Investiguer pourquoi `signals.md` du 25/04 n'a pas été généré ce matin | Quand un 2e jour consécutif manque le fichier (un seul jour = bruit, deux = pattern) |

## Edge — Ce qu'on pique cette semaine

**Hybrid search pgvector + tsvector + RRF** ([Supabase docs](https://supabase.com/docs/guides/ai/hybrid-search)). Notre `match_memories()` ne fait que cosine — sur des requêtes contenant un nom propre exact (slug wiki, nom de fichier, identifiant de projet), la recall plonge si l'embedder n'a pas vu le terme à l'entraînement. Le pattern 2026 stabilisé : BM25/tsvector top-100 + dense top-100 + Reciprocal Rank Fusion k=60 → 91% recall@10, sans cross-encoder, en SQL pur Postgres natif. La doc Supabase fournit la fonction RRF clé en main. **On ne le pique pas cette semaine** — S1+S2 sont prioritaires —, mais on remonte P2 → P28 avec la trace technique pour exécution rapide la semaine prochaine. (Source : `jarvis/intel/2026-04-25-veille.md` Axe 2.)

## Tendance

Jarvis va mieux qu'il y a 7 jours. La boucle d'apprentissage est rétablie (extraction_rate 0 → 3.8), 165 commits ont matérialisé Phase 7 méta-doc + Architecture viewer, et le rechallenging fonctionne (1/3 SHIP rétrogradé cette semaine = 33%, au-dessus de la cible 20% — sain après 7.1% historique). La friction restante est concentrée sur les capteurs eux-mêmes : `extract_signals.py` voit l'envers du miroir, et un unload LM Studio peut encore brûler 55 minutes sans alerte. Les 2 SHIP de cette semaine ferment précisément ces deux trous.

## Méta-tendance (mensuelle)

*Pas premier audit du mois — section non remplie cette semaine.*

---

## Dernière MAJ

- **2026-04-25** : audit v13. 2 SHIP (KNOWN_SECTIONS resync + LM Studio unload mid-run detection), 1 KILL (legacy log + close stale preflight), 1 SHIP rétrogradé (project_status.yaml → P27), 4 PARK ajoutés (P27 status YAML, P28 hybrid search, P29 Mem0g graph, P30 missing signals 25/04).
