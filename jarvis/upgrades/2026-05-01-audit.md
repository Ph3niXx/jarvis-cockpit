# Audit Jarvis — 2026-05-01 (v23, post-burst UX 30/04)

> **Contexte de cadence** : 1ère routine de mai, 1er jour férié (Fête du Travail FR). Jean a enchaîné une session UX de 8 commits le 30/04 entre 22h23 et 23h13 (a11y prefers-reduced-motion, cap pulses infinis, kbd-fab toggle, skip link WCAG, zero-state streak sidebar, jarvis-lab palette aliasée, OAuth overlay theme-aware, hero compact toggle), après le drainage v22 (S1 panel-history, S2 panel-opps, K1 archive prompts v21) en début de soirée. Bilan : streak commits 30/04 = 11 commits, vraiment tenu. **Mais** la dette d'archivage gonfle en miroir : 7 fichiers untracked/modified sur le scope cowork + 1 migration SQL `sql/013_jobs_inherit_status.sql` non commitée et probablement non appliquée en prod, 3 prompts v22 toujours en pending alors que les 3 commits sont mergés.

## North Star de la semaine

**Cette semaine, on fixe les filets de sécurité livrés cet automne (migration jobs trigger appliquée + claude panel restauré dans la nav) plutôt que d'ouvrir un nouveau front : la dette de "code livré mais pas fini d'intégrer" doit retomber à zéro avant le prochain sprint UX.**

## Ce qui a bougé depuis le dernier audit (v22, 30/04 09:34)

- **Commits depuis v22 (11 au total, tous le 30/04 19h31 → 23h13)** :
  - `69ea05b` 19:31 panel-history guards défensifs — **v22-S1 SHIPPED**
  - `ce34742` 19:36 panel-opportunities guards w.urgency — **v22-S2 SHIPPED**
  - `27738a8` 19:39 archive 4 prompts v21 + commit audit v21 — **v22-K1 SHIPPED**
  - `2d61267` 22:23 a11y kill-switch global prefers-reduced-motion (couvre 9 animations infinies)
  - `4bf1874` 22:27 a11y cap 10 pulses infinis à 3 cycles
  - `708b6fc` 22:34 cockpit masque kbd-fab après 7j + toggle profil
  - `3a50a6d` 22:45 cockpit zero-state streak sidebar (message d'amorçage)
  - `5b06741` 22:50 a11y skip link "Aller au contenu" WCAG 2.4.1
  - `4033320` 22:56 cockpit jarvis-lab palette `--jl-*` aliasée sur tokens globaux
  - `073e22a` 23:02 fix(auth) overlay OAuth respecte thème stocké avant mount React
  - `bc1e146` 23:13 cockpit toggle compact/plein du hero + persistance localStorage
- **Shipped (v22)** : 2 SHIPs + 1 KILL drainés en 8 minutes le 30/04 soir = 100% v22 exécuté. Burst UX en seconde partie de soirée = momentum réel mais hors backlog v22 (proactif Jean, pas via routine audit).
- **Friction résiduelle critique (NOUVELLE, à fermer ce tour)** :
  - `sql/013_jobs_inherit_status.sql` **untracked** (trigger Postgres BEFORE INSERT pour neutraliser les republications LinkedIn). Dette critique : DDL sur disque, jamais committed → la prod soit n'a pas le trigger, soit l'a en avance sur le code. Discussion Cowork avec Jean lit déjà 554 lignes `jobs` (vs 174 documenté précédemment) avec 4 status — donc soit (a) Jean a appliqué le trigger directement en prod sans commit local, soit (b) la migration attend toujours. Dans les 2 cas, l'absence de commit = bug de cohérence repo.
  - 4 fichiers modifiés non commités : `cockpit/styles-jarvis-lab.css`, `docs/cowork-routines/jobs-radar.md`, `docs/specs/index.json`, `docs/specs/tab-jobs.md` (édité avec mention du trigger 013). Ces edits documentent la migration mais ne peuvent pas être commités sans la migration elle-même.
  - 2 fichiers untracked routine : `audits/2026-04-30-design-audit.md`, `docs/veille-claude/ecosystem-2026-04-30.md`. Pattern P37 connu (déjà parké réveil 5 mai), résiduel tolérable encore 4 jours.
  - 3 prompts v22 toujours dans `pending/` (`2026-04-30-S1-fix-history-typeerror.md`, `S2-fix-opps-urgency-null.md`, `K1-hygiene-v21.md`) alors que les 3 commits sont mergés (`69ea05b`, `ce34742`, `27738a8`). Pattern auto-référentiel récidive — v22-K1 a archivé les 4 prompts v21 mais s'est commit avant d'archiver ses propres prompts. **Exact même pattern qu'avant**.
  - `INDEX.md` n'a pas le statut `shipped` sur les lignes v22-S1, v22-S2, v22-K1 (toujours `proposed` au moment de cet audit).

## Signaux faibles (télémétrie + logs)

- **Top 5 events 7j** (figés sur `2026-04-29-signals.md`, dernière extraction 29/04 20:14) : `section_opened` (468), `jobs_action` (84), `error_shown` (26), `recent_filter_auto_on` (18), `pipeline_triggered` (15). **Pas de signals.md plus récent** : les pipelines n'ont pas tourné depuis le 29/04 18:14 (3 jours), Jean n'a pas démarré `start_jarvis.bat` depuis. Cohérent avec férié 1er mai et activité dev 30/04 soir uniquement.
- **Sections les plus revisitées** : `brief` (38), `updates` (34), `top` (33), `jarvis-lab` (27), `week` (24).
- **Sections jamais ouvertes** : `claude` — **3 extractions consécutives = vrai signal**. Diagnostic en lecture code ce tour : la route `claude` existe ([cockpit/app.jsx:497](cockpit/app.jsx)), le panel partage `PanelVeille` avec 5 autres, le data-loader fetch `articles WHERE section='claude'` ([data-loader.js:1230](cockpit/lib/data-loader.js)), le spec [docs/specs/tab-claude.md](docs/specs/tab-claude.md) mentionne explicitement "Clic sidebar « Claude » dans le groupe Veille", et `data-claude.js` est en place. **MAIS** `cockpit/nav.js::Veille` ne contient PAS d'entrée `{ id: "claude", ... }` — elle a été silencieusement perdue dans le refactor `004f55a` ("NAV en source unique") qui consolidait `data.js::nav` → `nav.js::COCKPIT_NAV`. Conséquence : panel orphelin, accessible uniquement via deeplink `#claude`. → **S1 ce tour-ci**.
- **Erreurs UI (semaine glissante)** :
  - `panel:jobs` 11 occ. `WebSocket not available` → résolu par v21-S2 (commit `7935da5`). Validation prod **toujours en attente** car pas de signals.md post-fix.
  - `panel:history` 8 occ. `TypeError toLocaleString` → résolu par v22-S1 (commit `69ea05b`). Validation prod **toujours en attente**.
  - `jarvis:gateway` 7 occ. tunnel intermittent — pas un bug code, hors scope.
  - `panel:opps` (estimation) 24 occ. (chiffre v22) → résolu par v22-S2 (commit `ce34742`). Validation prod en attente.
- **`llm_traces.jsonl`** : dernier event `2026-04-29 18:14:41`, 14201 ms, status:ok. **5+ jours sans incident LLM** (règle 8 sur le seuil "3 jours" de P32 réinitialisé). v15-S1 + v14-S1 tiennent.
- **Erreurs pipelines (PRE-fix v21-S3)** :
  - `last_indexer.log` (29/04 18:13) : 3 erreurs 409 sur `memories_vectors`. Fix `8f3764d` mergé 22:40 mais pipeline jamais re-runné. **Validation v21-S3 toujours en attente**.
  - `last_activity_brief.log` (29/04 18:14) : 1 erreur 409 sur `(date)=(2026-04-28)`. Idem.
- **Méta-tendance audit** : v22 a tenu 100% (3/3 SHIPs/KILL drainés en 8 min). v23 doit fermer la dette d'intégration *sans* ouvrir un sujet UX neuf — Jean l'a déjà fait en autonomie le 30/04 soir, parfait. La discipline Cowork doit suivre, pas devancer.

## SHIP (max 3, après rechallenging)

### S1 — Restaurer `{ id: "claude" }` dans `cockpit/nav.js::Veille`

**Pourquoi maintenant** : sert directement la North Star (filet de sécurité livré, intégration finie). Le panel "Claude" a été techniquement complètement implémenté (route + panel partagé + data-loader + corpus + spec + pipelines articles) mais perdu de la sidebar dans un refactor — pendant 3+ extractions, "claude" est resté la seule "section jamais ouverte" alors que le contenu existe et que Jean utilise massivement Claude au quotidien (4ème section `jarvis-lab` = 27 ouvertures, mais zéro pour `claude`). 1 ligne à ajouter. Effort dérisoire vs valeur info récupérée.
**Diagnostic** : [cockpit/nav.js:28-34](cockpit/nav.js) — groupe `Veille` contient 6 entrées (`updates`, `veille-outils`, `sport`, `gaming_news`, `anime`, `news`). Manque `{ id: "claude", label: "Claude", icon: "bot" }` (icon historique avant `004f55a`). Diff `git log -p -S '"claude"' -- cockpit/data.js` (commit `004f55a` 25/04) montre la suppression silencieuse de l'entrée pendant la consolidation NAV vers source unique. Le route `app.jsx:497` est intact (`<PanelVeille corpus="CLAUDE_DATA" title="Claude" actorsLabel="canaux Anthropic" categoryLabel="Source" typeLabel="Format" prodSection={null} />`).
**Définition de "fait"** : après modification, l'entrée "Claude" apparaît dans la sidebar groupe Veille (5e position entre `updates` et `veille-outils`, ordre logique original), un clic émet `section_opened claude`, le panel charge depuis `articles WHERE section='claude'`. À J+7, `2026-05-08-signals.md` doit montrer `section_opened` claude > 0.
**Effort** : XS (~5 min — 1 ligne nav.js + sw.js cache bump auto via GitHub Action).

#### Prompt Claude Code — S1

```
Contexte projet : panel "Claude" (route updates/sport/.../claude) techniquement
en place mais perdu de la sidebar dans le refactor `004f55a` (NAV en source
unique) — 3 extractions signals.md consécutives marquent "section claude jamais
ouverte" alors que le route, le panel-veille corpus, le data-loader et
docs/specs/tab-claude.md sont prêts. Le spec dit explicitement "Clic sidebar
« Claude » dans le groupe Veille" — la régression est silencieuse.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, écris un rapport ~15 lignes après ces
vérifications :

1. Lis `cockpit/nav.js` lignes 20-50 EN ENTIER. Confirme que le groupe Veille
   contient EXACTEMENT 6 entrées dans cet ordre : updates, veille-outils, sport,
   gaming_news, anime, news. Aucune mention de "claude".

2. Lis `cockpit/app.jsx` lignes 494-510. Confirme la route :
   `else if (activePanel === "claude") content = <PanelVeille corpus="CLAUDE_DATA" ...`

3. Lis `cockpit/lib/data-loader.js` ligne 1230. Confirme le loader :
   `async claude(){ return once("claude_articles", () => q("articles", ...))`

4. Confirme la présence de `cockpit/data-claude.js` (corpus stub `CLAUDE_DATA`)
   et son chargement dans `index.html` :
   `grep -n "data-claude.js" index.html`

5. Lis `docs/specs/tab-claude.md` (les 30 premières lignes). Confirme que le
   parcours utilisateur commence par "Clic sidebar « Claude » dans le groupe
   Veille".

6. Vérifie l'icône utilisée historiquement avant `004f55a` :
   `git log --all -p -S '"claude"' -- cockpit/data.js | grep -A1 "id.*claude" | head -5`
   → l'historique doit révéler l'icône précédente (probablement "bot" ou
   "sparkles"). Si "bot" n'existe pas dans le système d'icônes courant
   (`cockpit/icons.jsx`), tomber sur l'icône utilisée pour `updates`/Veille IA
   ("sparkles") ou pour le panel Jarvis ("assistant") selon ce qui matche le
   mieux le contenu (Anthropic releases/SDK).

Écris un rapport et ATTENDS ma validation explicite. Le rapport doit indiquer
quel id d'icône valide tu vas utiliser et où exactement l'entrée s'insère
(entre `updates` et `veille-outils`, position 2 du groupe).

Objectif : restaurer la navigation vers le panel Claude, sans toucher au reste.

Fichiers concernés :
- cockpit/nav.js (modification, 1 ligne)

Étapes (après validation Phase 0) :
1. Insérer dans le groupe "Veille" (après `updates`, avant `veille-outils`) :
   `    { id: "claude", label: "Claude", icon: "<icon validé en Phase 0>" },`
2. Aucune autre modification.

Contraintes :
- 1 seule ligne ajoutée. Pas de refacto, pas de réordonnancement, pas de
  changement d'icône sur les autres entrées.
- Pas de modification de `app.jsx`, `data-loader.js`, `data-claude.js`,
  `panel-veille.jsx`, `index.html`, ou des specs.
- Le sw.js doit être resync via `node scripts/sync-sw.mjs` OU laissé à la
  GH Action `sw-sync` (préférence : laisser CI le faire pour valider le pipeline
  auto-sync v21).

Validation (lance ces commandes après modification) :
- `grep -A2 "Veille" cockpit/nav.js | grep "claude"` → doit retourner la ligne.
- `grep -c "id: \"claude\"" cockpit/nav.js` → doit retourner 1.
- Demander à Jean : ouvrir le cockpit en local OU sur GH Pages, vérifier la
  sidebar groupe Veille → entrée "Claude" présente, clic ouvre le panel et
  émet `section_opened claude`.

Ne fais PAS :
- Ne touche pas à `cockpit/data.js` (legacy mort, consultatif seulement).
- N'ajoute pas de count, unread, ou badge sur l'entrée — pas de signal pour ça.
- Ne change pas l'ordre des autres entrées Veille.
- Ne push pas après commit.

Quand c'est fait : montre le diff complet AVANT git add. git commit avec message
`fix(cockpit): restaure entrée "Claude" dans sidebar Veille (régression 004f55a)`.
PAS de push.
```

### S2 — Commiter + appliquer la migration `sql/013_jobs_inherit_status.sql`

**Pourquoi maintenant** : sert directement la North Star (intégration finie). Migration DDL **untracked** depuis ≥ 2 jours (jobs panel a déjà absorbé 380 nouvelles lignes archived selon le diff `tab-jobs.md`, donc Jean utilise déjà le trigger en prod, OU il documente avant d'appliquer). Dans les deux cas, le repo est désynchronisé du cluster Supabase. Risque : un autre poste qui clone le repo n'aura pas le trigger en prod — ou pire, la migration est appliquée d'un côté seulement et la doc ment.
**Diagnostic** : [sql/013_jobs_inherit_status.sql](sql/013_jobs_inherit_status.sql) untracked (`git status --porcelain`). Trigger PL/pgSQL `BEFORE INSERT` qui hérite du `status` (`archived`/`snoozed` ≤30j/≤7j) et `user_notes` quand une republication LinkedIn matche `(lower(trim(title)), lower(trim(company)))`. CREATE OR REPLACE FUNCTION + DROP IF EXISTS = 100% idempotent. Modifications jointes : `docs/specs/tab-jobs.md` mentionne déjà la migration et la table à 554 lignes (vs 174 dans la version commitée).
**Définition de "fait"** : `git log -1 --stat sql/013_jobs_inherit_status.sql` retourne le commit. La migration est appliquée en prod (vérifiable via `mcp__6b08b413-..._list_migrations` ou en testant : INSERT d'une offre titre+boîte = ligne archivée existante < 30j → la nouvelle ligne hérite `status='archived'`).
**Effort** : S (~30 min — commit immédiat + apply migration via MCP Supabase + smoke test trigger).

#### Prompt Claude Code — S2

```
Contexte projet : `sql/013_jobs_inherit_status.sql` est untracked depuis ≥ 2
jours. Le diff staged sur `docs/specs/tab-jobs.md` documente déjà le trigger ET
mentionne 554 lignes `jobs` (vs 174 dans la version commitée précédente),
suggérant que Jean a déjà rempli la table mais peut-être pas appliqué le
trigger en prod. Risque : repo dit "trigger appliqué" mais Supabase soit
n'a pas le trigger, soit l'a en avance sur le commit. Migration idempotente
(CREATE OR REPLACE + DROP IF EXISTS).

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de commiter ou appliquer quoi que ce soit, écris un rapport ~25 lignes
après ces vérifications :

1. `git status --porcelain` doit afficher (au moins) :
   - `?? sql/013_jobs_inherit_status.sql`
   - ` M docs/specs/tab-jobs.md`
   - ` M docs/specs/index.json`
   - ` M docs/cowork-routines/jobs-radar.md`
   - ` M cockpit/styles-jarvis-lab.css` (sans rapport, à laisser hors de ce SHIP)

2. `cat sql/013_jobs_inherit_status.sql` — confirmer 100% idempotence
   (CREATE OR REPLACE FUNCTION + DROP IF EXISTS TRIGGER) et absence
   d'opération destructive.

3. Vérifier l'état du trigger côté prod via MCP Supabase :
   - `mcp__6b08b413-..._list_migrations` → lister les migrations appliquées,
     repérer si `013_jobs_inherit_status` ou similaire y figure.
   - Si MCP non disponible : `mcp__6b08b413-..._execute_sql` avec :
     `SELECT tgname FROM pg_trigger WHERE tgname = 'jobs_inherit_user_status';`
     → 1 ligne = trigger déjà en prod, 0 ligne = pas appliqué.

4. Vérifier le contenu actuel de la table `jobs` :
   `mcp__6b08b413-..._execute_sql`:
   `SELECT count(*) AS total, count(*) FILTER (WHERE status = 'archived') AS archived FROM public.jobs;`
   → confirmer 554/399 (ou chiffre actuel) cohérent avec le diff `tab-jobs.md`.

5. Lis `docs/specs/tab-jobs.md` lignes 80-95 (diff staged) pour confirmer la
   description du trigger correspond exactement à la migration.

Écris un rapport et ATTENDS ma validation explicite. Le rapport doit conclure
sur 1 des 3 verdicts :
  (a) "Trigger déjà appliqué en prod, repo en retard → seul commit local
      manquant. Action : git add + commit, pas d'apply."
  (b) "Trigger pas appliqué, repo en avance → seul apply prod manquant.
      Action : commit + apply migration."
  (c) "Désynchro indéterminée (MCP Supabase non lisible) → 2 actions :
      (1) commit immédiat pour figer le repo, (2) demander à Jean de vérifier
      manuellement et d'appliquer si besoin."

Objectif : repo + prod alignés sur la migration 013.

Fichiers concernés :
- sql/013_jobs_inherit_status.sql (commit untracked)
- docs/specs/tab-jobs.md (commit modif staged)
- docs/specs/index.json (commit modif staged — bump last_updated tab-jobs)
- docs/cowork-routines/jobs-radar.md (commit modif staged)

Étapes (après validation Phase 0) :
1. Si verdict (a) ou (b) ou (c) : git add sql/013_jobs_inherit_status.sql
   docs/specs/tab-jobs.md docs/specs/index.json docs/cowork-routines/jobs-radar.md
2. NE PAS git add cockpit/styles-jarvis-lab.css (hors scope ce SHIP).
3. git commit avec message
   `feat(jobs): trigger jobs_inherit_user_status — neutralise republications LinkedIn`
4. Si verdict (b) : appliquer la migration via
   `mcp__6b08b413-..._apply_migration` avec le contenu de sql/013_*.sql
   et le nom `jobs_inherit_user_status`.
5. Si verdict (c) : NE PAS apply, écrire un message explicite à Jean dans
   le diff de commit ("MCP Supabase non lisible — vérifier manuellement et
   appliquer la migration si pg_trigger ne contient pas jobs_inherit_user_status").
6. Smoke test trigger (si appliqué dans cette session) :
   `mcp__6b08b413-..._execute_sql`:
   ```
   BEGIN;
   INSERT INTO public.jobs (linkedin_job_id, title, company, status)
   VALUES ('test-013-' || gen_random_uuid()::text, 'TEST_S2_AUDIT_REPRO',
           'TEST_S2_AUDIT_REPRO', 'archived');
   INSERT INTO public.jobs (linkedin_job_id, title, company, status)
   VALUES ('test-013-' || gen_random_uuid()::text, 'TEST_S2_AUDIT_REPRO',
           'TEST_S2_AUDIT_REPRO', 'new');
   SELECT status FROM public.jobs WHERE title = 'TEST_S2_AUDIT_REPRO'
     ORDER BY created_at DESC LIMIT 1;
   -- Expected: 'archived' (hérité du trigger)
   ROLLBACK;
   ```
   → si le SELECT retourne 'archived', trigger OK.

Contraintes :
- Pas de modification du fichier .sql (idempotence préservée).
- Pas de migration parallèle créée (014, 015...).
- Pas de DROP TABLE, pas de TRUNCATE.
- L'apply migration via MCP Supabase est idempotent (CREATE OR REPLACE).
- Smoke test entouré de BEGIN ... ROLLBACK (pas de pollution table).
- Pas de push.

Validation (lance ces commandes après modification) :
- `git log -1 --stat` → 4 fichiers : sql/013, tab-jobs.md, index.json, jobs-radar.md.
- `git status --porcelain | grep -E "sql/013|tab-jobs|index.json|jobs-radar"`
  → ne doit plus rien afficher.
- Si verdict (b) ou apply effectué : `mcp__6b08b413-..._execute_sql`
  `SELECT tgname, tgrelid::regclass FROM pg_trigger WHERE tgname = 'jobs_inherit_user_status';`
  → 1 ligne avec `tgrelid = public.jobs`.

Ne fais PAS :
- Ne touche pas à `cockpit/styles-jarvis-lab.css` ni aux 3 prompts pending v22
  (hors scope ce SHIP, K1 s'en occupe).
- N'apply pas la migration sans verdict explicite — toujours préférer le
  commit-then-apply à apply-then-commit.
- Ne push pas.

Quand c'est fait : montre le diff complet AVANT git add. git commit comme spécifié.
PAS de push.
```

### S3 — *Pas de S3 ce tour-ci.*

**Justification du non-S3** : 2 SHIPs + 1 KILL, tous strictement orientés "filet de sécurité / intégration finie". Le burst UX du 30/04 soir (8 commits a11y) a déjà saturé la cadence cockpit pour la semaine — ouvrir un 3e chantier (genre diagnostic engagement 1% link_clicked, ou audit panel claude habillage post-réintégration) reviendrait à empiler. Cartouche gardée pour v24 (lundi prochain probable, après le pont 1er-2-3 mai).

## SHIP rétrogradés pendant le rechallenging

**Aucun ce tour-ci.** Justification ci-dessous (ratio mensuel actuel 18.6%, sous cible 20%, et v22 = 0 rétrogradé). **Méta-test honnête** : sur 2 SHIPs, lequel serait le plus faible ?
- S1 (restore claude nav) : 1 ligne, valeur info évidente (panel orphelin réactivé). Risque ≈ 0. Inattaquable.
- S2 (commit+apply 013) : opération de prod via MCP, donc supérieurement risquée. Mais idempotente, encadrée par Phase 0 stricte avec 3 verdicts (a/b/c) et smoke test BEGIN...ROLLBACK. Le risque résiduel est l'erreur opérationnelle pure (Jean doit valider avant apply), pas le SHIP lui-même.

Le plus faible est S2 par exposition opérationnelle, mais le rétrograder priverait Jean de la sortie de désynchro repo/prod — risque pire. Maintenu.

**Sur le ratio mensuel** : si v24 (semaine prochaine) ne rétrograde toujours rien, **rétrograder le SHIP le plus faible des 3**. Engagement formel.

#### Rechallenging — S1

- **Red team** : "Tu remets une entrée nav alors que peut-être Jean l'a *intentionnellement* retirée pour épurer le groupe Veille." → Faux : la suppression a eu lieu pendant `004f55a` ("NAV en source unique") qui était un refactor de plomberie, pas une décision UX. Le commit message ne mentionne pas la suppression de "claude". Le spec `tab-claude.md` mentionne explicitement la sidebar comme parcours principal. La régression est documentée par le silence du commit message. Objection insuffisante.
- **Contre-exemple** : "Jean réactive le panel et se rend compte que les articles `section='claude'` sont vides ou doublonnent `veille-outils`." → Plausible. Mais : `data-loader.claude()` fetch jusqu'à 200 articles avec `limit=200`, et `main.py` mentionne explicitement "10 sections dont claude" — le pipeline alimente bien cette section. Si vide, c'est une autre dette à creuser, pas une raison de garder le panel orphelin. Pas pire que rien.
- **Coût d'opportunité** : 5 min. Alternatives : (a) refactor data-history transformHistory (P25 dérivé), (b) explorer l'engagement 1% link_clicked. Aucune des deux n'a la valeur "réactiver une section morte" en 5 min. S1 reste meilleur usage.
- **Futur soi** : "J'aurais dû voir que l'ajout de la nav a réveillé une section qui n'a jamais eu de raison d'exister depuis 2 mois." → Très improbable, la section a été pensée et alimentée depuis le début, juste cachée par accident. Rien à signaler.
- **Cohérence historique** : aucun audit antérieur n'a parké/killé cette idée. v22 a noté "section claude jamais ouverte" comme pattern stable sans diagnostic. v23 livre le diagnostic et le fix dans le même tour = workflow standard. RAS.
- **Verdict final : MAINTENU**

#### Rechallenging — S2

- **Red team** : "Tu touches à la prod via MCP sans Jean au clavier — un audit autonome qui exécute une migration est dangereux." → Vraie objection. Mitigée par : Phase 0 stricte avec 3 verdicts explicites, refus d'apply en cas d'incertitude (verdict c), smoke test BEGIN...ROLLBACK uniquement. Mais surtout : ce SHIP **délègue l'exécution à Claude Code** (le prompt est un input pour la prochaine session), pas à Cowork audit. Donc Jean valide la Phase 0 avant tout apply. Risque nominal.
- **Contre-exemple** : "Le trigger est déjà en prod, l'apply le re-créé proprement, mais Jean a entre-temps ajouté un trigger manuel customisé qui se fait écraser." → Très improbable (1 seul utilisateur, pas de DBA tiers). Et CREATE OR REPLACE ne casse pas si rien d'incompatible existe. Mais si Jean veut un test différent, il pourra rejeter le verdict (b) et choisir (c).
- **Coût d'opportunité** : 30 min. Alternatives : (a) écrire la nav claude immédiatement (fait via S1), (b) drainer pending v22 (fait via K1). Aucune ne couvre la désynchro repo/prod. S2 reste critique.
- **Futur soi** : "J'aurais dû appliquer la migration plus tôt et éviter les 2 jours de gap où le code front parle d'un trigger qui n'existe pas." → Si la prochaine routine Cowork (jobs-radar) insère une republication LinkedIn et observe que le `status='new'` au lieu de `status='archived'` hérité, on saura que le trigger n'était pas appliqué. Signal exploitable. Pas de regret asymétrique.
- **Cohérence historique** : aucun PARK/KILL n'a déjà visé cette migration (créée hier 30/04). RAS.
- **Verdict final : MAINTENU**

#### Rechallenging — K1 (court : tests 1+4)

- **Red team** : "Tu refais un K1 d'hygiène pour la 5e fois en 1 mois sans automatiser. P36+P37 dorment encore — tu valides juste le pattern d'inertie." → P37 réveille le 5 mai (dans 4 jours). Si v23-K1 commit + déplace + sync, et que **le pattern revient à v24** (semaine prochaine), P37 doit déclencher. K1 = trim ponctuel le 1er mai, P37 = solution structurelle programmée. Tolérable, mais surveillé.
- **Futur soi** : "J'ai fait le geste manuel pour la N-ième fois alors qu'un script de 20 lignes l'aurait évité." → Vrai. Mais entre 1er-5 mai = 4 jours de pont férié + week-end, peu probable que Jean code. K1 garde le repo propre pendant ce gap.
- **Verdict final : MAINTENU**

## KILL list

### K1 — Hygiène v22 : drain 3 prompts pending → done/, sync INDEX (v22-S1/S2/K1 = `shipped`), commit 2 artefacts cowork untracked

**Raison** : exact pattern auto-référentiel — `2026-04-30-S1-fix-history-typeerror.md`, `S2-fix-opps-urgency-null.md`, `K1-hygiene-v21.md` sont dans `pending/` alors que les 3 commits correspondants (`69ea05b`, `ce34742`, `27738a8`) sont mergés depuis 30/04 19h31-19h39. Plus : `audits/2026-04-30-design-audit.md` et `docs/veille-claude/ecosystem-2026-04-30.md` untracked (P37 condition pas encore éveillée mais résiduel à fermer en passant). Plus : `INDEX.md` ne marque pas v22-S1/S2/K1 en `shipped`.

**Risque de regret** : Faible. `done/` est l'archive standard, le commit des artefacts cowork est strictement additif, le sync INDEX est documentation à jour.

**Rechallenging court** : voir bloc ci-dessus (test 1 + test 4).

**Prompt Claude Code** :

```
Phase 0 (4 commandes lit-only) :

1. ls jarvis/upgrades/prompts/pending/ | sort
   → doit lister EXACTEMENT (sauf nouveaux v23 que l'extraction Phase 7.5 va
     créer en parallèle) :
     2026-04-30-K1-hygiene-v21.md
     2026-04-30-S1-fix-history-typeerror.md
     2026-04-30-S2-fix-opps-urgency-null.md

2. git log --oneline --since="3 days ago" | grep -E "(69ea05b|ce34742|27738a8)"
   → doit retourner les 3 commits correspondants. Si un manque, STOP.

3. git status --porcelain | head -15
   → doit afficher (au moins) :
     ?? audits/2026-04-30-design-audit.md
     ?? docs/veille-claude/ecosystem-2026-04-30.md

4. grep -nE "v22.*proposed|2026-04-30.*proposed" jarvis/upgrades/INDEX.md
   → doit retourner les 3 lignes v22 (S1, S2, K1) toujours en `proposed`.

Si tout est OK :

5. git mv jarvis/upgrades/prompts/pending/2026-04-30-{K1-hygiene-v21,S1-fix-history-typeerror,S2-fix-opps-urgency-null}.md jarvis/upgrades/prompts/done/

6. Réécrire jarvis/upgrades/prompts/README.md pour ne lister que les nouveaux
   v23 prompts en pending (la Phase 7.5 du SCRIPT audit du 1er mai aura déjà
   ajouté ses lignes — cohérence à valider en lisant le fichier après extract).

7. Mettre à jour `jarvis/upgrades/INDEX.md` :
   - Ligne v22-S1 : `proposed` → `shipped` + feedback `Commit 69ea05b`
   - Ligne v22-S2 : `proposed` → `shipped` + feedback `Commit ce34742`
   - Ligne v22-K1 : `proposed` → `shipped` + feedback `Commit 27738a8`
   - Section Stats : bumper "v22: 2 SHIP shippés (S1+S2) + 1 KILL (K1)" et
     reflux sur le total mensuel.

8. git add -A audits/2026-04-30-design-audit.md
   docs/veille-claude/ecosystem-2026-04-30.md
   jarvis/upgrades/

9. git commit -m "chore(jarvis): v23-K1 — archive 3 prompts v22 + sync INDEX (v22 100% shipped) + commit cowork orphans"

Validation :
- ls jarvis/upgrades/prompts/pending/ → ne contient AUCUN fichier *.md du 30/04
  (peut contenir les nouveaux 2026-05-01-S1, S2 v23 pending).
- git log -1 --stat → ≥6 fichiers déplacés/ajoutés/modifiés.
- git status --porcelain | grep -E "audits|veille-claude/ecosystem-2026-04-30"
  → vide.

Ne fais PAS :
- Ne supprime aucun fichier (utilise git mv, pas rm).
- Ne touche pas aux 4 fichiers `M` hors scope (cockpit/styles-jarvis-lab.css,
  docs/cowork-routines/jobs-radar.md, docs/specs/index.json, docs/specs/tab-jobs.md)
  — ils sont liés à S2 (sql/013), pas K1.
- Ne push pas.

Affiche le diff complet AVANT git add (sauf renames qui apparaissent dans
git status comme R).
```

## PARK

| Idée | Condition de réveil |
|------|---------------------|
| **P33 — Fix wiki TypeError `e.word_count.toLocaleString`** *(v22-S1 a déjà résolu panel-history TypeError, panel wiki distinct mais même pattern)* | Si `wiki` apparaît avec ≥10 `error_shown TypeError` sur signals.md du 8/05. Distinct de panel-history. |
| **P35 — Fix `panel:opps` TypeError `null.urgency`** *(v22-S2 a résolu)* | À retirer de PARK après validation prod (≤1 occurrence dans signals.md du 8/05). |
| P36 — `audit_commit.sh` + règle Cowork SKILL | Si à 12 mai (2 sem post v16-S1 mergé), audits Jarvis restent partiellement non commités. v22-K1 + v23-K1 = 2 patches ponctuels en 2 jours, pas une solution. Si à v25 même pattern, force la promotion. |
| P37 — Auto-commit batch des 4 routines Cowork | Réveil **5 mai** (dans 4 jours). Si v24 audit du 5/05 affiche encore ≥2 untracked dans `audits/` ou `docs/veille-claude/`, promouvoir SHIP. v23-K1 commit les 2 du 30/04 pour réinitialiser le compteur. |
| P38 — Phi-4-mini-instruct (split slot LM Studio) | Quand 1 nuit nightly_learner échoue OR latence p90 > 60s sur `_compact_history` 3j. Compteur reset au 26/04, **5j tenus**. |
| P39 — Outlook Graph + msal `tenant=consumers` | Si observer COM crashe ≥ 2x/sem OR Jean travaille un jour sans Outlook desktop. |
| P40 — VectorChord-BM25 + RRF dans retriever.py | Quand un retrieval test rate par cosine pure ET P38 shippé. |
| P29 — Mem0g graph extension | P40 shippé ET ≥ 3 contradictions de faits en 1 semaine. |
| P32 — Cloud fallback `nightly_learner._llm_extract` | Si après v15-S1 mergé (5j), ≥ 3 nuits sans extraction. Compteur reset à `566a084`. |
| P27 — Bump `project_status.yaml` Phase 7 méta-doc | Quand les 4 lints CI Phase 7 tournent 7j sans warning. |
| P30 — Investiguer pourquoi `signals.md` du 30/04 + 01/05 manquant *(condition triple-remplie depuis le 26/04)* | À traiter dès qu'une fenêtre se libère. v15-S2 (`run_nightly_after_deps.bat` couplant signals à start_jarvis.bat) couvre quand Jean démarre. Le gap actuel = Jean n'a pas démarré pendant 3j (cohérent férié + week-end). Si à v25 (lundi 4/05) signals.md du jour absent, P30 promouvoir. |

**Note pattern résolu ce tour** : la "section claude jamais ouverte" passait pour un signal d'engagement à creuser dans v22 — diagnostic livré aujourd'hui (régression `004f55a`), résolution via S1. Pas une nouvelle PARK, retirée du tracking.

## Edge — Ce qu'on pique cette semaine

**Rien à voler.** La veille du 30/04 a confirmé 10 nouveautés (mcp-azure-devops, mcp-snowflake, mcp-bigquery, mcp-databricks, superpowers-skills, vercel-skills-cli, mcp-server-dev-plugin, claude-code-channels, etc.) — tous des inbound MCP servers + skills. Aucun ne résout un problème UX panel-claude ou un problème de désynchro repo/prod (le scope de cette routine). Si le superpowers-skills framework devait mériter un tour propre, ce serait via PARK P41 nouveau ("Évaluer agentic methodology framework superpowers vs current Cowork workflow") — pas dans la NS d'archivage de cette semaine.

## Tendance

**Jarvis va opérationnellement et techniquement mieux qu'il y a 7 jours.** Côté technique : 5 jours sans incident LLM (record), v22 100% shippé en 8 minutes le 30/04 soir, burst UX a11y de 8 commits (kill-switch reduced-motion, cap pulses, kbd-fab toggle, skip link WCAG, zero-state streak, palette aliasée, OAuth theme-aware, hero compact). Côté opérationnel : la cadence Cowork audit a tenu (v22 émis matin, drainé soir). **Risque résiduel** : la dette d'archivage gonfle quand Jean code le soir sans Cowork (artefacts cowork le matin = file pending qui s'accumule sans que le code-side du repo ne s'en occupe). v23-K1 commit + P37 si récidive à v25 = piste structurelle.

## Méta-tendance (mensuelle, à remplir le 1er audit du mois)

**Aujourd'hui = 01/05 = 1er audit de mai → cette section est remplie.**

- **Avril ferme sur 36 SHIP shippés** (v1→v22 dont v22 100%). Bilan mensuel : excellent.
- **8 SHIP rétrogradés** sur le mois. Ratio mensuel **8/44 ≈ 18.2%** (cible 20%). Sous cible mais marginalement. **Engagement v23 → v24** : si v24 ne rétrograde toujours rien, force la rétrogradation du SHIP le plus faible.
- **North Star la plus récurrente en avril** : "fiabiliser stack LM Studio / éliminer timeouts silencieux" — 5 audits sur 9 (25/04 → 28/04 + segments du 29/04). Suivie de "fenêtre UX" (3 audits : 22, 23). North Star résolue depuis 5 jours sans incident. **Mai s'ouvre sur** : "filets de sécurité" (intégration finie des dettes accumulées).
- **Bilan PARK** : 14 actifs en fin avril. P37 réveille **dans 4 jours** (5 mai), surveillé. P30 condition triple-remplie depuis 26/04 mais reste différé tant que `start_jarvis.bat` couvre (v15-S2). P38/P39/P40 dorment.
- **Métrique nouvelle** : "jours consécutifs avec ≥1 commit". Streak record en avril : 2 jours (29-30/04). Aujourd'hui 01/05 = férié, streak probable cassée. À surveiller : redémarrage routine 4 ou 5 mai.
- **Mai = ouverture** : NS prévues = (1) automatisation boucle archivage (P36/P37 réveils 5-12 mai), (2) exploration sélective des PARKs M (P38 si pression LLM revient, P40 si retrieval rate observé), (3) ouverture d'une route "skills" si Jean veut intégrer superpowers-skills ou mcp-server-dev-plugin.

---

## Dernière MAJ

- **2026-05-01 (v23, post-burst UX 30/04 + post-drainage v22)** : 1ère routine de mai, jour férié 1er mai. v22 a tenu 100% (3/3 SHIPs/KILL drainés en 8 min le 30/04 19h31-19h39), suivi d'un burst UX autonome de 8 commits a11y/cosmétique entre 22h23 et 23h13. **2 SHIP émis** (S1 restaure entrée nav.js "claude" — régression silencieuse de `004f55a` détectée par signals.md "section claude jamais ouverte" stable sur 3 extractions / S2 commit + apply migration `sql/013_jobs_inherit_status.sql` jobs republication LinkedIn) + **1 KILL** (hygiène v22 — 3 prompts pending → done/ + sync INDEX v22 shipped + commit 2 artefacts cowork untracked + sync README/INDEX). Pas de S3 (cartouche). 0 SHIP rétrogradé au rechallenging (ratio mensuel 18.2%, sous cible — engagement v24 = forcer si toujours 0). Bilan mensuel d'avril : 36 SHIP shippés, 8 rétrogradés, 14 PARKs actifs, North Star LLM stabilisée 5 jours. Engagement v24 : si à la prochaine routine la cadence est tenue (S1+S2+K1 mergés) on confirme la transition vers la NS "automatisation boucle archivage" (réveil P37 le 5/05).
