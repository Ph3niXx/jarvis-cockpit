# Audit Jarvis — 2026-04-30 (v22, post-drainage v21)

> **Contexte de cadence** : 1ère routine après le drainage massif d'hier soir (29/04 20h-23h), où 7 commits ont enchaîné v21-S1, v21-S2, v21-S3, v21-K1, puis 3 commits opportunistes (`ee2a344` sw auto-sync, `4dbc662` refactor stacks, `7bd0e20` .nojekyll). La semaine s'ouvre avec un backlog réellement digéré côté code, mais 4 prompts toujours dans `pending/` + audit v21 untracked = la dette d'archivage est à fermer avant de saturer un nouveau chantier.

## North Star de la semaine

**Cette semaine, on solde les 2 dernières dettes UX visibles dans le cockpit (P33 panel:history TypeError + P35 panel:opps urgency null) ET on ferme proprement le cycle v21 (audit committed + 4 prompts pending → done/) — la fenêtre UX-focused promise depuis 4 audits est ouverte parce que la stack LLM est saine depuis 4 jours et qu'aucun chantier majeur n'est en cours.**

## Ce qui a bougé depuis le dernier audit (v21, 29/04 18:29)

- **Commits depuis v21 (10 au total, dont 7 utiles entre 20h08 et 23h11)** :
  - `eadf772` 20:08 sync INDEX stats v20 (déjà v21 production)
  - `b4e5804` 20:41 fix `status_generator` get_chunks_count via Range header — **v21-S1 SHIPPED**
  - `7935da5` 20:44 CSP wss:// pour Supabase Realtime — **v21-S2 SHIPPED**
  - `8f3764d` 22:40 sb_post on_conflict — **v21-S3 SHIPPED**
  - `5e83774` 22:43 kill data-jobs.js — **v21-K1 SHIPPED**
  - `ee2a344` 22:48 sw.js auto-sync STATIC manifest + GH Action sw-sync (NOUVEAU, post-v21 — sortait sw obsolète qui shippait styles.css?v=22 vs v=23 réel sur PWA mobile)
  - `4dbc662` 23:08 refactor stacks (kill `window.prompt/confirm/alert` → modale React thémée)
  - `7bd0e20` 23:11 `.nojekyll` (fix build GitHub Pages qui crashait sur Liquid template literal)
- **Shipped** : 4 SHIP/KILL v21 + 3 commits hygiène opportunistes = **drainage complet en 3h**.
- **Friction résiduelle** :
  - **4 prompts v21 toujours dans `pending/`** (S1, S2, S3, K1) malgré tous les commits mergés. Pattern `pending/` → `done/` non automatisé persiste.
  - **`2026-04-29v21-audit.md` untracked** + `INDEX.md` modifié non commité + `prompts/README.md` modifié non commité.
  - **Logs pipelines stales** (`last_indexer.log` du 29/04 18:13 = pré-fix S3 v21) — pas encore de validation prod du fix `on_conflict`. Le run nightly du 30/04 06:24 UTC n'a pas semblé tourner ce matin (logs non rafraîchis à 07:20 UTC). Probable : Jean n'a pas encore lancé `start_jarvis.bat`.
  - **`signals.md` du 30/04 absent** (mais c'est cohérent avec un démarrage Jarvis tardif — pas urgence).

## Signaux faibles (télémétrie + logs)

- **Top 5 events 7j** (figés sur `2026-04-29-signals.md`, sandbox sans accès direct Supabase — P26) : `section_opened` (468), `jobs_action` (84), `error_shown` (26), `recent_filter_auto_on` (18), `pipeline_triggered` (15). Total 7j : **649 events**.
- **Sections les plus revisitées** : `brief` (38), `updates` (34), `top` (33), **`jarvis-lab` (27)**, `week` (24). `jarvis-lab` confirme sa 4e place — donc le fix S1 v21 (Chunks: 0 → 1006 vérifié dans `last_status_gen.log`) bénéficie à un panel réellement consulté.
- **Sections jamais ouvertes** : `claude` (encore — pattern stable, pas un faux négatif post-déploiement). À surveiller : pourquoi Jean n'ouvre-t-il jamais ce panel alors qu'il consulte massivement Claude API/SDK ? Possible mismatch entre nom "Claude" et contenu "Anthropic + Claude Code + SDK"). Pas SHIP ce tour, mais à creuser.
- **Erreurs UI captées (semaine glissante)** :
  - `panel:jobs` 11 occurrences `WebSocket not available: The operation is insecure.` → **résolu par v21-S2** (commit `7935da5`). Compte du jeudi 30/04 attendu < 3 résiduels post-déploiement. À vérifier dans `signals.md` du 6/05.
  - `panel:history` 8 occurrences `TypeError: Cannot read properties of undefined (reading 'toLocaleString')` → diagnostic confirmé en lecture code : `cockpit/panel-history.jsx:444,449` accède `t.total_articles.toLocaleString` et `t.total_jarvis_calls.toLocaleString` sans guard. Source : `cockpit/lib/data-loader.js:4623-4628` n'écrase `HISTORY_DATA.totals` que **si `arts60.length > 0`** — sinon le fallback de `cockpit/data-history.js:175-180` reste mais peut être obsolète selon ordre de chargement Tier 1/Tier 2. → **S1 ce tour-ci**.
  - `jarvis:gateway` 7 occurrences `Failed to fetch | localhost:8765` (tunnel Cloudflared / localhost down). Pattern réseau intermittent — pas un bug code, hors action ce tour.
- **`llm_traces.jsonl` état actuel** : 148 traces totales, 25 erreurs cumulées (dont 20 APITimeoutError, 2 model_unloaded, 2 inference_stuck, 1 APIConnectionError). **Dernière erreur : 2026-04-26 08:40 UTC = 4 jours pleins sans incident LLM.** v15-S1 + v14-S1 tiennent. La récente trace 29/04 18:14 (chars/latency normaux) confirme que la stack a survécu sans accroc à la session de drainage v20+v21 chargée.
- **Erreurs pipelines** (logs pré-fix S3 v21) :
  - `last_indexer.log` (29/04 18:13) : 3 erreurs 409 sur `memories_vectors` (clés `wiki_concepts/agents-ia/0`, `rte_usecases/.../0`, `user_profile/user_profile_all/0`).
  - `last_activity_brief.log` (29/04 18:14) : 1 erreur 409 sur `(date)=(2026-04-28)`.
  - **Validation S3 v21 EN ATTENTE** : on ne pourra confirmer que le fix `sb_post(on_conflict=...)` tue les 4 erreurs/run qu'au prochain run nightly de Jean (06:24 UTC, demain ou après).
- **`last_status_gen.log` (29/04 18:40)** : `Chunks: 1006`, `Commits ce mois: 86`. **Validation S1 v21 confirmée** — la fonction `get_chunks_count()` retourne 1006 vs 0 avant. Panel Jarvis Lab affiche maintenant le bon chiffre.
- **`last_nightly_learner.log` (29/04 06:24)** : run propre, 256s, 7 faits + 16 entités, 100% upsert. Tendance saine.
- **Méta-tendance audit** : v20 + v21 = sprint exceptionnel (5 prompts drainés v20, 3 SHIP + 1 KILL émis ET shippés v21 dans la même journée). v22 doit éviter de saturer la cadence — d'où max 2 SHIPs UX cette semaine, pas 3.

## SHIP (max 3, après rechallenging)

### S1 — Fix `panel:history` TypeError sur `t.total_articles.toLocaleString`

**Pourquoi maintenant** : sert directement la North Star. **8 occurrences `error_shown` cette semaine** (top 2 des erreurs UI, derrière le panel jobs WebSocket maintenant résolu par v21-S2). P33 a été parké le 26/04 avec condition de réveil double `remonte > 50 occ./sem OU prochaine fenêtre UX-focused`. La 2e condition est explicitement remplie : NS de cette semaine = fenêtre UX, stack LLM saine depuis 4 jours, aucun chantier M en cours. Ratio "agacement / coût" élevé (8 erreurs visibles / 30 min de fix).
**Diagnostic** : `cockpit/panel-history.jsx:444` `{t.total_articles.toLocaleString("fr-FR")}` et `:449` `{t.total_jarvis_calls.toLocaleString("fr-FR")}` plantent quand `t.total_articles` ou `t.total_jarvis_calls` sont `undefined`. Cause amont : `cockpit/lib/data-loader.js:4623-4628` n'hydrate `HISTORY_DATA.totals` **que si `arts60.length > 0`** — sinon le fallback de `cockpit/data-history.js:175-180` reste actif, mais la garde sur les champs n'est pas garantie côté consommateur React.
**Définition de "fait"** : après modification, ouvrir le panel Historique sur Firefox (qui déclenchait l'erreur) avec un cas où `arts60` est vide n'émet plus d'event `error_shown` `panel:history`. `signals.md` du 6/05 ou 7/05 confirme ≤ 2 occurrences résiduelles (avant déploiement) sur la semaine.
**Effort** : XS (~30 min — 4 lignes de code).

#### Prompt Claude Code — S1

```
Contexte projet : 8 erreurs `error_shown panel:history` cette semaine
(2e pattern d'erreur UI). Message :
`TypeError: Cannot read properties of undefined (reading 'toLocaleString')`.
Cause : `cockpit/panel-history.jsx:444,449` accède `t.total_articles.toLocaleString`
et `t.total_jarvis_calls.toLocaleString` sans guard, alors que `t = hist.totals`
peut avoir ces champs `undefined` quand `cockpit/lib/data-loader.js:4623-4628`
ne mute pas `HISTORY_DATA.totals` (cas où la requête `articles` retourne 0 lignes).

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, écris un rapport ~25 lignes après ces
vérifications :

1. Lis `cockpit/panel-history.jsx` lignes 420-465 EN ENTIER. Confirme :
   (a) ligne 426 `const t = hist.totals;` et que `hist = window.HISTORY_DATA;` ligne 356,
   (b) toutes les occurrences de `t.xxx.toLocaleString` (au moins lignes 444, 449),
   (c) tout autre accès non gardé sur `t.xxx.yyy` (`t.peak_day.short_label`,
       `t.peak_day.articles`, `t.streak_days`, `t.total_actions`).

2. Lis `cockpit/lib/data-loader.js:4615-4630`. Confirme la garde
   `if (window.HISTORY_DATA && arts60.length)` — donc si la table `articles`
   est vide ou que la requête échoue, `HISTORY_DATA.totals` garde la valeur
   initiale du fallback `cockpit/data-history.js`.

3. Lis `cockpit/data-history.js:175-205`. Note le shape exact du fallback :
   `total_articles`, `total_jarvis_calls`, `total_actions`, `streak_days`,
   `peak_day` doivent y être. Vérifie qu'ils existent tous.

4. Trace l'ordre de chargement Tier 1 → Tier 2 :
   `grep -n "data-history\|HISTORY_DATA" cockpit/lib/data-loader.js cockpit/lib/bootstrap.js index.html`
   → identifie quand `data-history.js` est chargé vs quand `loadPanel("history")`
   est appelé. Hypothèse à confirmer : si `loadPanel` mute partiellement (ex:
   `days` mis à jour mais pas `totals` parce que Promise.all interrompue), on
   se retrouve avec un état incohérent.

5. Confirme côté code que les 4 occurrences hors `toLocaleString` aussi accèdent
   sans guard : ligne 454 `t.peak_day.short_label`, 455 `t.peak_day.articles`.
   `t.peak_day` peut être null/undefined si `days = []`.

Écris un rapport et ATTENDS ma validation explicite.

Objectif : panel Historique cesse d'émettre `error_shown panel:history` quand
`HISTORY_DATA.totals` a un champ `undefined`. Approche défensive en aval (guards
au point de consommation), pas refonte amont.

Fichiers concernés :
- cockpit/panel-history.jsx (modification, lignes 426-461)

Étapes (après validation Phase 0) :
1. Remplacer `const t = hist.totals;` par
   `const t = hist?.totals || {};` ligne 426.
2. Pour chaque accès `t.xxx.toLocaleString(...)` (lignes 444, 449), entourer
   par `(t.total_articles ?? 0).toLocaleString("fr-FR")` et
   `(t.total_jarvis_calls ?? 0).toLocaleString("fr-FR")`.
3. Pour `t.peak_day.short_label` et `t.peak_day.articles` (lignes 454-455),
   utiliser optional chaining + fallback : `t.peak_day?.short_label || "—"`
   et `t.peak_day?.articles ?? 0`.
4. Pour `t.streak_days` (450) et `t.total_actions` (459), `t.streak_days ?? 0`
   et `t.total_actions ?? 0`.
5. Pour la division ligne 445 `(t.total_articles / hist.days.length)`,
   protéger : `((t.total_articles ?? 0) / Math.max(1, hist?.days?.length || 1)).toFixed(0)`.

Contraintes :
- Aucun changement dans `data-loader.js` ni `data-history.js`. Le fix est
  strictement défensif côté consommateur.
- Pas de nouvelle dépendance, pas de refacto opportuniste hors lignes 426-461.
- Pas de `useState` ajouté ni de side-effect.
- Pas de modification de la structure JSX (DOM identique).

Validation (lance ces commandes après modification) :
- `grep -n "toLocaleString" cockpit/panel-history.jsx` → les 2 occurrences
  doivent maintenant être protégées par `?? 0`.
- `grep -nE "t\.(total_articles|total_jarvis_calls|peak_day|streak_days|total_actions)" cockpit/panel-history.jsx`
  → chaque accès doit être protégé (`?.` ou `??`).
- Demander à Jean : ouvrir le panel Historique dans Firefox, console devtools
  ouverte → aucune erreur `TypeError` ne doit apparaître, même si on simule un
  état dégradé en commentant temporairement la requête `arts60` dans
  data-loader.js.

Ne fais PAS :
- Ne touche pas à `cockpit/lib/data-loader.js` (transformHistory ou la garde
  `arts60.length`) — c'est hors scope ce SHIP.
- Ne refonds pas `HISTORY_DATA` en TypeScript ou avec PropTypes.
- N'ajoute pas un boundary React global — il existe déjà au niveau App.
- Ne push pas après commit.

Quand c'est fait : montre le diff complet AVANT git add. git commit avec message
`fix(cockpit): panel-history — guards défensifs sur totals undefined (P33 résolu)`.
PAS de push.
```

### S2 — Fix `panel:opps` TypeError sur `w.urgency`

**Pourquoi maintenant** : sert directement la North Star. P35 parké le 27/04 avec condition `Prochaine fenêtre UX-focused (cogroupé P33)`. Condition remplie ce tour-ci. Le bug est en lecture code clairement actif : `cockpit/panel-opportunities.jsx:130-134` et `:196-201` accèdent `w.urgency` sans optional chaining, alors que les autres sites du même fichier (322, 378, 379, 407, 408) utilisent déjà `?.` — incohérence interne au fichier.
**Diagnostic** : `cockpit/panel-opportunities.jsx:130` `const w = opp.window;` puis `:132-134` `w.urgency === "closing"` → si `opp.window === null/undefined`, plante. Idem ligne 165 `w.closes_in`, 167 `w.closes_iso`, 176-177. La table `weekly_opportunities` peut contenir des lignes sans `window` (champ JSONB optionnel).
**Définition de "fait"** : après modification + déploiement, ouvrir le panel Opportunités sur une opportunité sans `window` (ou simuler en base) n'émet plus de TypeError. Vérification en Phase 0 que ce bug est encore actif aujourd'hui (24 occurrences = chiffre du 27/04, à confirmer/infirmer).
**Effort** : XS (~30 min).

#### Prompt Claude Code — S2

```
Contexte projet : `cockpit/panel-opportunities.jsx:130-134` et `196-201`
accèdent `w.urgency` sans optional chaining, alors que `w = opp.window` peut
être null si la table `weekly_opportunities` a une ligne avec `window: null`.
Les sites 322, 378-379, 407-408 du même fichier utilisent déjà `opp.window?.urgency`
— incohérence à corriger pour aligner FlagshipCard et OppCard sur le même
défensivisme.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, écris un rapport ~25 lignes après ces
vérifications :

1. Lis `cockpit/panel-opportunities.jsx` lignes 125-205 EN ENTIER. Note tous
   les accès non gardés à `w.xxx` (urgency, closes_in, closes_iso, opens).
   Compare avec lignes 320-410 où `?.` est appliqué partout.

2. Vérifie qu'au moins une opportunité en base a réellement `window: null` ou
   `window: undefined` :
   `grep -rn "window\b" jarvis/migrations/*.sql sql/ | grep -i "opportun"`
   puis lis le DDL de `weekly_opportunities`. Si `window` est NOT NULL, le bug
   est théorique et probablement non actif → STOP, retourner verdict "P35
   condition non remplie".

3. Si `window` est NULL-able, regarde si une ligne actuelle peut être null :
   le `gen_random_uuid()` insertion de `weekly_analysis.py` initialise-t-il
   toujours `window` ? Lis le pipeline qui upsert dans cette table :
   `grep -rn "weekly_opportunities" weekly_analysis.py jarvis/ pipelines/` puis
   note le shape. Si certaines opportunités historiques ont `window: null` (ex:
   premier run du pipeline), le bug est actif.

4. Vérifie que les autres accès dans le fichier n'introduisent pas de nouveau
   non-guard :
   `grep -nE "w\.(urgency|closes|opens)" cockpit/panel-opportunities.jsx`
   → produire la liste complète et catégoriser : déjà gardé (?.) vs non gardé.

5. Cherche dans signals.md ou logs récents si l'erreur `panel:opps` apparaît
   réellement cette semaine :
   `grep -n "panel:opps\|panel:opportunities" jarvis/intel/2026-04-29-signals.md`
   → 0 ou faible occurrence = bug théorique, retourner P35 PARK.
   Si > 0 occurrence = bug actif, continuer.

Écris un rapport et ATTENDS ma validation explicite. Le verdict de Phase 0
peut être : (a) "bug actif, fix go", (b) "bug théorique mais incohérence du
fichier mérite d'être alignée pour cohérence interne", (c) "P35 condition non
remplie, retourner PARK".

Objectif : aligner FlagshipCard (lignes 128-192) et OppCard (lignes 195-...)
sur le même défensivisme `?.` que les sites 322+.

Fichiers concernés :
- cockpit/panel-opportunities.jsx (modification, lignes 130-204)

Étapes (après validation Phase 0, et seulement si verdict (a) ou (b)) :
1. Remplacer `const w = opp.window;` (lignes 130 et 196) par
   `const w = opp.window || {};`. Ainsi `w.urgency` redevient `undefined` au
   lieu de planter — tous les `===` comparent à `"closing"`/`"getting_late"` etc.
   et retombent sur `false`, qui est le comportement attendu (defaultcase final).
2. Pour les accès dans le JSX (lignes 165, 167, 176-177) :
   - `{w.closes_in}` → `{w.closes_in || "—"}`
   - `{w.closes_iso ? ... : "pas de deadline"}` reste OK (le test garde la branche).
   - `{URGENCY_LABEL[w.urgency]}` → `{URGENCY_LABEL[w.urgency] || URGENCY_LABEL.right_time}`
   - `{w.opens?.replace("S", "")}` reste OK.
3. Aucun autre changement.

Contraintes :
- Pas de modification du DDL `weekly_opportunities` (RLS, schéma, NOT NULL).
- Pas de modification de `weekly_analysis.py` ni des transformers data-loader.
- Pas de refacto cosmétique ailleurs dans le fichier.
- Backward compat : aucun autre comportement n'est changé.

Validation (lance ces commandes après modification) :
- `grep -nE "w\.(urgency|closes_in|closes_iso|opens)" cockpit/panel-opportunities.jsx | grep -v "\\?\\." | grep -v "||"`
  → ne doit retourner QUE des accès dans des contextes inoffensifs (test
    conditionnel `w.closes_iso ?` ou similaire). Aucun accès "lecture brute"
    sans guard ne doit subsister.
- Demander à Jean : ouvrir le panel Opportunités → aucune erreur console.
  Si une opportunité de test sans `window` est insérée :
  `INSERT INTO weekly_opportunities (week, title, window, ...) VALUES (..., NULL, ...)`,
  le panel affiche "—" / "pas de deadline" sans planter.

Ne fais PAS :
- Ne touche pas au DDL Supabase.
- Ne refacto pas FlagshipCard ou OppCard pour mutualiser leur logique window.
- Ne change pas les fonctions `formatWindow()` (35-39) ni `windowProgress()` (42-48).
- Ne push pas.

Quand c'est fait : montre le diff complet AVANT git add. git commit avec message
`fix(cockpit): panel-opportunities — guards w.urgency sur opp.window null (P35 résolu)`.
PAS de push.
```

### S3 — *Pas de S3 ce tour-ci.*

**Justification du non-S3** : la cadence v20+v21 a déjà absorbé 5 prompts drainés et 7 commits dans une journée. Émettre un 3e SHIP demain alors que la stack est saine, le backlog code digéré et les conditions PARK restantes (P36/P37/P38/P39/P40) toutes non remplies = saturation gratuite. Mieux vaut garder la cartouche S3 pour la semaine prochaine quand un signal nouveau émergera.

## SHIP rétrogradés pendant le rechallenging

**Aucun ce tour-ci.** Justification : seulement 2 SHIPs émis (S1 + S2), tous deux XS, tous deux conditionnés par Phase 0 stricte (S2 peut basculer en PARK retour si bug théorique non actif). Méta-test honnête : si Phase 0 de S2 montre que `weekly_opportunities.window` est NOT NULL ou que 0 opportunité réelle a `window: null` en base, le SHIP s'auto-rétrograde. C'est exactement le rôle de la Phase 0 — pas de rétrogradation pré-Phase 0 pour un SHIP qui passe les 5 tests adversariaux.

**Méta-test sur la non-rétrogradation cumulative** : ratio mensuel actuel 7/40 ≈ 17.5% (sous cible 20%). Si **le prochain audit (v23, semaine prochaine) n'en rétrograde toujours pas**, je force la rétrogradation du SHIP le plus faible des 3 — par discipline. Aujourd'hui, sur 2 SHIPs, le plus faible serait S2 (effet Phase 0 = auto-rétrogradation conditionnelle), donc le rééquilibrage se fait *de facto* via le test de Phase 0. Acceptable.

## KILL list

### K1 — Hygiène v21 : ranger 4 prompts pending → done/, commiter audit v21 untracked, mettre à jour README

**Raison** : 4 prompts (`2026-04-29-S1-fix-chunks-zero.md`, `S2-csp-wss-supabase.md`, `S3-sb-post-on-conflict.md`, `K1-kill-data-jobs-mock.md`) sont dans `jarvis/upgrades/prompts/pending/` alors que les 4 commits correspondants (`b4e5804`, `7935da5`, `8f3764d`, `5e83774`) sont mergés depuis hier soir. Le pattern auto-référentiel exact que v15-K1 et v17-K1 visaient à éliminer se reproduit. Plus : `jarvis/upgrades/2026-04-29v21-audit.md` est untracked. Plus : `INDEX.md` et `prompts/README.md` sont modifiés non commités (M dans git status).

**Risque de regret** : Faible. `done/` est l'archive standard. Le commit du audit v21 est strictement additif.

**Rechallenging court** :
- Test 1 (red team) : "Tu refais un K1 d'hygiène pour la 4e fois en 1 mois — soit tu automatises (P36/P37) soit tu acceptes que c'est juste comme ça." → Les 2 PARKs P36 et P37 ont des conditions de réveil futures (12 mai pour P36, 5 mai pour P37). Ce K1 n'est pas une solution structurelle, c'est un trim ponctuel. Acceptable car P37 va se réveiller dans 5 jours et P36 dans 12.
- Test 4 (futur soi) : "J'ai fait le geste manuel pour la N-ième fois sans rien automatiser." → Si à v23 (semaine prochaine) le pattern revient, P37 doit absolument déclencher (condition 7 jours = 5 mai). C'est la prochaine action structurelle.

**Prompt Claude Code** :

```
Phase 0 (3 commandes lit-only) :

1. ls jarvis/upgrades/prompts/pending/ | sort
   → doit lister EXACTEMENT (parmi d'autres) ces 4 fichiers v21 :
     2026-04-29-K1-kill-data-jobs-mock.md
     2026-04-29-S1-fix-chunks-zero.md
     2026-04-29-S2-csp-wss-supabase.md
     2026-04-29-S3-sb-post-on-conflict.md

2. git log --oneline --since="2 days ago" | grep -E "(b4e5804|7935da5|8f3764d|5e83774)"
   → doit retourner les 4 commits correspondants. Si un commit manque, STOP
   et rapporter.

3. git status --porcelain | head -10
   → doit afficher (au moins) :
     M jarvis/upgrades/INDEX.md
     M jarvis/upgrades/prompts/README.md
     ?? jarvis/upgrades/2026-04-29v21-audit.md
     ?? jarvis/upgrades/prompts/pending/

Si tout est OK :

4. git mv jarvis/upgrades/prompts/pending/2026-04-29-{K1-kill-data-jobs-mock,S1-fix-chunks-zero,S2-csp-wss-supabase,S3-sb-post-on-conflict}.md jarvis/upgrades/prompts/done/

5. Réécrire jarvis/upgrades/prompts/README.md pour ne lister AUCUN prompt en
   pending (puisque tous les v22 prompts seront créés à part — voir Phase 7.5).
   Garder le titre, le tableau vide, et le paragraphe "Workflow".

6. git add -A jarvis/upgrades/

7. git commit -m "chore(jarvis): v22-K1 — archive 4 prompts v21 shippés + commit audit v21 + sync README/INDEX"

Validation :
- ls jarvis/upgrades/prompts/pending/ → ne doit lister AUCUN fichier *.md v21
  (peut contenir les nouveaux v22 fichiers que la Phase 7.5 va créer).
- git log -1 --stat → doit afficher 4 fichiers déplacés (renames pending→done),
  1 fichier ajouté (2026-04-29v21-audit.md), et 2 modifications (INDEX, README).

Ne fais PAS :
- Ne supprime aucun fichier (utilise git mv, pas rm).
- Ne touche pas à `done/` au-delà des 4 mv.
- Ne push pas.

Affiche le diff complet AVANT git add (sauf renames qui apparaissent juste dans
git status).
```

## PARK

| Idée | Condition de réveil |
|------|---------------------|
| **P33 — Fix wiki TypeError `e.word_count.toLocaleString`** *(re-numbered comme S1 ce tour)* | À retirer de PARK après merge S1. |
| **P35 — Fix `panel:opps` TypeError `null.urgency`** *(promu en S2 ce tour)* | À retirer de PARK après merge S2 (ou re-PARK si Phase 0 montre bug théorique). |
| P36 — `audit_commit.sh` + règle Cowork SKILL | Si à 12 mai (2 sem post v16-S1 mergé), audits Jarvis restent partiellement non commités. v22-K1 commit l'audit v21 manuellement = 1 patch ponctuel de plus, pas une solution. |
| P37 — Auto-commit batch des 4 routines Cowork | Si à 5 mai (7 jours post v21), pattern "audits/veille/ecosystem orphans" persiste. |
| P38 — Phi-4-mini-instruct (split slot LM Studio) | Quand 1 nuit nightly_learner échoue OR latence p90 > 60s sur `_compact_history` 3j. |
| P39 — Outlook Graph + msal `tenant=consumers` | Si observer COM crashe ≥ 2x/sem OR Jean travaille un jour sans Outlook desktop. |
| P40 — VectorChord-BM25 + RRF dans retriever.py | Quand un retrieval test rate par cosine pure ET P38 shippé. |
| P29 — Mem0g graph extension | P40 shippé ET ≥ 3 contradictions de faits en 1 semaine. |
| P32 — Cloud fallback `nightly_learner._llm_extract` | Si après v15-S1 mergé (4 jours), ≥ 3 nuits sans extraction. Compteur reset à `566a084`. |
| P27 — Bump `project_status.yaml` Phase 7 méta-doc | Quand les 4 lints CI Phase 7 tournent 7j sans warning. |

**Note pattern observé** : la section "claude" reste systématiquement absente des sections ouvertes (2 extractions consécutives = vrai signal, pas faux négatif post-déploiement). Pas de SHIP ce tour parce que pas diagnosticé, mais à creuser dans un audit futur — possible mismatch entre nom "Claude" et contenu effectif (Anthropic + Claude Code + SDK Python/TS + Agent SDK + skills). Hypothèses : (a) Jean utilise déjà Claude API quotidiennement et ne sent pas le besoin d'une page de veille dédiée ; (b) le nom du panel n'évoque pas son contenu ; (c) le panel duplique de l'info présente ailleurs (veille-outils, updates). Si pattern stable v22→v25, créer un PARK P41 "Diagnostic abandon panel claude".

## Edge — Ce qu'on pique cette semaine

**Rien à voler.** La veille du 29/04 a déjà donné 3 PARKs (P38/P39/P40), tous avec conditions d'éveil futures. Cette semaine = solder UX existant, pas ouvrir un nouveau front. Si un projet open-source publié dans les 7 derniers jours résout un problème UX précis (ex: bibliothèque "react-error-boundary-defensive-react-fragments"), ça pourrait valoir un coup d'œil. Mais le problème UX panel:history est de 4 lignes — autant le fixer en place que d'importer une dépendance.

## Tendance

**Jarvis va opérationnellement et techniquement mieux qu'il y a 7 jours.** Côté technique : 4 jours sans incident LLM, fix sb_post on_conflict mergé (validation prod attendue dès demain matin), CSP wss whitelisté (panel jobs WebSocket résolu), status_generator répare son compteur (1006 chunks vs 0). Côté opérationnel : v20 a drainé un backlog vieux de 4 jours en 1 session, v21 a re-cassé la streak git silence avec 7 commits utiles, v22 = 1ère semaine post-drainage avec backlog code à zéro. Le rechallenging adversarial a *prouvé sa valeur* (la pause v18+v19 a déclenché le drainage v20).

**Risque résiduel** : la dette d'archivage (prompts pending non rangés, audit untracked) reste *manuelle*. P36 et P37 ont des conditions claires (12 mai et 5 mai) — si Jean les force avant le réveil naturel, on automatise. Sinon, on attend.

## Méta-tendance (mensuelle, à remplir le 30/04 ou 01/05)

**Aujourd'hui = 30/04 = dernier jour d'avril → cette section est remplie.**

- **Avril ferme sur 35 SHIP shippés** (v1→v21 dont 4 v21 + 3 hygiène opportunistes 29/04 soir). Bilan de cadence remarquable.
- **8 SHIP rétrogradés** sur le mois (v7-S3 → P17, v10-S3 → P20, v10-S1 → P31, v13-S3 → P27, v14-S3 → P32, v14-S4 → P33, v16-S2 → P36, v17 = 0 émis = 0 rétrogradé). Plus 0 rétrogradé en v21 et v22 = ratio mensuel **8/43 ≈ 18.6%**, sous cible 20%. Signal de tolérance, à surveiller en mai. Si à v23 ou v24 je n'en rétrograde toujours pas, force la rétrogradation du SHIP le plus faible.
- **North Star la plus récurrente en avril** : "fiabiliser la stack LM Studio / éliminer les timeouts silencieux" — 5 audits sur 8 (25/04 → 28/04 + segments du 29/04). Récurrence = signe de focus *et* signe de problème structurel résolu progressivement par strates (v13-S2 → v14-S1 → v15-S1). Le problème est désormais clos (4 jours sans incident).
- **Bilan PARK** : 14 actifs en fin avril (P26→P40 hors P31 killed). 3 nouveaux PARKs depuis veille 29/04 (P38/P39/P40). Inflation modérée mais conditions de réveil claires.
- **Métrique nouvelle ce mois** : "jours consécutifs avec ≥1 commit". Streak en cours = 2 jours (29/04 + 30/04 si v22-K1 commit). Record de silence cassé le 29/04 après 83h.
- **Mai = ouverture** : NS prévues = automatisation de la boucle archivage (P36/P37 réveils 5-12 mai), exploration sélective des PARKs M (P38 si pression LLM revient, P40 si retrieval rate observé).

---

## Dernière MAJ

- **2026-04-30 (v22, post-drainage v21)** : 1ère routine après le sprint exceptionnel 29/04 (v20 drainage + v21 émission/exécution dans la même journée, 7 commits utiles entre 20h08 et 23h11). 2 SHIP émis (S1 fix panel:history TypeError P33 promu / S2 fix panel:opps TypeError P35 promu — sous condition Phase 0 confirme bug actif), 1 KILL (hygiène v21 — 4 prompts pending → done + commit audit v21 + sync INDEX/README). Pas de S3 (cartouche gardée). 0 SHIP rétrogradé au rechallenging (ratio mensuel 18.6% sous cible — à surveiller). Bilan mensuel d'avril : 35 SHIP shippés, 8 rétrogradés, 14 PARKs actifs, North Star LLM stabilisée. Engagement v23 : si à la prochaine routine la cadence est tenue (S1+S2 mergés) on confirme la transition vers la NS UX.
