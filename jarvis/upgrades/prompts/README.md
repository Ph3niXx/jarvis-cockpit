# Prompts Claude Code en attente

| Fichier | Titre | Effort | Date audit |
|---------|-------|--------|------------|

**Workflow** : copier-coller le contenu d'un fichier dans Claude Code, exécuter, revoir le diff, commit, puis déplacer le fichier vers `done/`.

**Pour cette itération** : aucun prompt en attente — backlog v15+v16 entièrement drainé le 2026-04-29 (v17-K1 dans `a64128a`, v16-S1 dans `3a1a63d`, v16-K1 dans `1c65373`, v15-S1 dans `566a084`, v15-S2 dans `89c6866`). Engagement audit v17 → v18 honoré.
