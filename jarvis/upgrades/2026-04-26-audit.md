# Audit Jarvis — 2026-04-26

## North Star de la semaine

Cette semaine, plus aucun timeout LM Studio ne doit consommer 55 minutes en silence — qu'il s'agisse d'un modèle déchargé OU d'une inférence bloquée par saturation VRAM —, et la dérive de `KNOWN_SECTIONS` doit être bloquée à la merge plutôt que rattrapée à l'audit suivant.

## Ce qui a bougé depuis le dernier audit

- **39 commits depuis le 25/04** (24h denses) : v13 complet shippé (S1 KNOWN_SECTIONS resync `e1b27bc`, S2 LM Studio mid-run unload `f82b959`, K1 stale log + close pending `dca2849`), refactor majeur `96e1a3c refactor(jarvis): switch LM Studio stack to single Qwen3.5 9B Instruct` (élimine la contention de slot LM Studio entre nightly et chat), nouveau panel `Veille outils` (`4d742f7`) + sous-onglet catalogue Claude Ecosystem (`6fbe418`) + 2 nouvelles tables Supabase (`claude_veille`, `claude_ecosystem`), passe UX shell complète (focus-visible, spacing tokens, skeleton, sidebar, top-cards, Morning Card hero "à traiter", quiet mode, command palette, Revue du jour, section Claude — `74bc2ee`, `aeb8f20`, `e9c2cb8`).
- **Shipped depuis 2026-04-25** :
  - v13-S1 — `e1b27bc` : KNOWN_SECTIONS resynchronisé (anime, brief, challenges, claude, gaming, gaming_news, history, ideas, jarvis, jarvis-lab, jobs, music, news, opps, perf, profile, radar, recos, review, search, signals, sport, stacks, top, updates, week, wiki).
  - v13-S2 — `f82b959` : `_sync_call` détecte maintenant `model_unloaded_midrun` via `check_lm_studio()` après timeout — abort en quelques secondes au lieu de 55 min sur le scénario "modèle déchargé".
  - v13-K1 — `dca2849` : legacy log `nightly_learner.log` supprimé + pending `2026-04-14-S1-preflight-lm-studio.md` déplacé en `done/`.
- **Friction résiduelle critique** :
  - **2026-04-25 14:46 → 15:25** : 3 timeouts × 1114s = **55 min brûlées en silence**, modèle `qwen/qwen3.5-9b` (nouvelle stack post-`96e1a3c`). Le préflight passe (modèle chargé), le check_lm_studio() après timeout aurait répondu `"ok"` (modèle toujours listé), donc la branche `model_unloaded_midrun` de v13-S2 ne déclenche pas. **Cause probable** : saturation VRAM sur la RTX 5070 Laptop 8 Go (LLM 9B Q4_K_M ~5.5 Go + embedding 0.6B Q8 ~640 Mo + KV cache → débordement Shared PCIe à 10-50× plus lent). v13-S2 a fermé une moitié du trou ; l'autre moitié reste ouverte.
  - **`signals.md` du 26/04 manquant** : la tâche planifiée Python à 05h30 UTC n'a (encore) pas généré le fichier d'aujourd'hui — 2e absence consécutive (déjà manqué le 25/04). Audit fait sans signaux du jour, en s'appuyant sur le fichier 25/04 + traces `llm_traces.jsonl` + git log.
  - **`KNOWN_SECTIONS` déjà obsolète** : panel `veille-outils` ajouté 3h après la resync v13-S1 (commit `4d742f7` à 18:45, resync à 15:25). Le pattern manuel a dérivé en moins de 4 heures.

## Signaux faibles (télémétrie + logs)

- **Top events 7j (signals 25/04, dernier disponible)** : `section_opened` (575), `error_shown` (154), `pipeline_triggered` (25), `profile_field_saved` (7), `jobs_action` (3). Sections les plus revisitées : `brief` (47), `top` (44), `radar` (30), `recos` (29), `music` (28).
- **Sections jamais ouvertes** : `claude` et `review` — mais ces 2 panels ont été pushés à 15:13 le 25/04, soit 11 min avant la génération du fichier signals. Faux négatifs probables : tester sur le prochain run.
- **Erreurs UI captées (semaine du 25/04)** :
  - `panel:wiki` : 108 occurrences (`Cannot read properties of undefined (reading 'toLocaleString')`). Source confirmée : `cockpit/panel-wiki.jsx:201` et `:333` accèdent `e.word_count.toLocaleString` sans guard. `lib/data-loader.js:380` calcule `wordCount = content.split(/\s+/).length` — donc c'est seulement undefined sur un chemin de fallback (probablement `WIKI_DATA` non hydraté). Bruit observable, hors North Star — listé en P33.
  - `panel:opps` : 24 occurrences (`Cannot read properties of null (reading 'urgency')`).
  - `panel:history` : 9 occurrences (`Cannot read properties of undefined (reading 'toLocaleString')`).
- **`llm_traces.jsonl` 25-26/04** :
  - 20 OK, 4 erreurs (1 `model_unloaded_midrun` à 13:32 — v13-S2 a fonctionné comme prévu, abort en 0 ms ; 3 `APITimeoutError` × 1114s à 14:46/15:06/15:25).
  - Latence p50/p90/max sur OK : 15.7s / 130.8s / 763.9s. Une réussite à 763.9s = 12.7 min — proche du timeout configuré (120s × ~6 = ?). À investiguer si récurrent.
  - 26/04 : 1 trace OK ce matin (chars_in 4, latency 25491 ms = 25s pour un ping minimal). Ce ping de 25s pour un prompt de 4 chars suggère que la VRAM est encore tendue au démarrage.
- **Erreurs pipelines** :
  - `last_indexer.log` : 3 erreurs 409 résiduelles (PARK P21 — sous le seuil de 5/run, pas d'action).
  - `last_nightly_learner.log` : log tronqué après `LLM retry 1/2 after APITimeoutError (backoff 2s)` (run du 26/04 09:42). Soit le run a fini après le retry (OK), soit il s'est planté silencieusement avant le `_write_trace` final. À recontrôler après v13-S2 sur 2-3 jours.
  - `last_activity_brief.log` : `Upsert failed: 409 ... activity_briefs_date_key` (24/04). Mineur — un upsert raté à cause d'une contrainte unique probablement déjà couvert par un retry plus haut, sous le seuil.
- **Métriques santé** : 883 vecteurs (+131 vs 23/04, +45 vs 25/04), 93 faits actifs, 1000 events télémétrie totaux. `status_generator.log` rapporte toujours `Chunks indexes: 0` malgré 883 vecteurs réels — bug confirmé dans `get_chunks_count()` (header `Content-Range` non honoré sur `limit=0` selon RLS courant). Hors scope cette semaine, à parker.
- **Méta-tendance audit** : v13-S1 manuel a dérivé en 3h. Les SHIPs cette semaine doivent prendre acte que le diagnostic est lui-même soumis à la même règle de drift que les specs `docs/specs/` — d'où le SHIP S2 ci-dessous.

## SHIP (max 3, après rechallenging)

### S1 — Détection `inference_stuck` dans `_sync_call` (modèle chargé mais inférence bloquée)

**Pourquoi maintenant** : sert directement la North Star. v13-S2 ferme le scénario "modèle déchargé". Reste le scénario "modèle chargé mais inférence bloquée" qui a brûlé 55 min le 25/04 (3 timeouts × 1114s) — exact même coût que la version unload, exact même cause symptomatique pour Jean. Sans ce SHIP, chaque session de chat avec contexte chargé risque la même perte silencieuse.
**Diagnostic** : `jarvis/llm_client.py:91-117` (boucle de retry `_sync_call`). Cf `jarvis_data/llm_traces.jsonl` 2026-04-25T14:46:13, T15:06:42, T15:25:16 — chacun `model: "qwen/qwen3.5-9b"`, `latency_ms ≈ 1114000`, `attempts: 3`, `error_type: "APITimeoutError"`. Cause probable : VRAM saturée RTX 5070 Laptop 8 Go (CLAUDE.md ligne explicite : "il reste ~1.9 Go pour le KV cache, OK tant que le contexte chat reste sous ~4k tokens").
**Définition de "fait"** : après modification, un nouveau APITimeoutError sur un modèle qui répond `"ok"` à `check_lm_studio()` (donc loaded mais stuck) produit un trace `status: "error", error_type: "inference_stuck"` et un `RuntimeError` levé à l'**attempt 1** (max ~240s wallclock au lieu de 1114s). `nightly_learner.state.last_result == "lm_studio_inference_stuck"`. `/health` renvoie `status: "degraded"`. Vérifié sur 1 run réel (saturation provoquée en lançant un long chat parallèle) ou simulé via mock.
**Effort** : S (1.5-2h)

#### Prompt Claude Code — S1

```
Contexte projet : Le 24/04 puis le 25/04, 3 timeouts × 1114s = 55 min brûlées 
en silence par jour. v13-S2 (commit f82b959) couvre désormais "modèle déchargé 
en cours de run" (model_unloaded_midrun). Reste à couvrir : "modèle chargé 
mais inférence bloquée" (probable VRAM debord sur RTX 5070 Laptop 8 Go avec 
le 9B + embedding 0.6B chargés simultanément). Quand ça arrive, check_lm_studio() 
renvoie "ok" (le modèle est listé) mais l'inférence elle-même est stuck — 
retry n'aide pas, c'est hardware, on ne sortira pas de la boucle de saturation 
en réessayant.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

Avant de modifier quoi que ce soit, compile ce bilan en ~30 lignes :

1. Lis jarvis/llm_client.py EN ENTIER (~133 lignes — fais-le).
2. Lis jarvis/server.py L130-170 (l'endpoint /health, la liste degraded markers).
3. Lis jarvis/nightly_learner.py L455-510 (les 2 catch existants pour 
   "lm_studio_unloaded_midrun" — ce sont tes templates).
4. tail -30 jarvis_data/llm_traces.jsonl | grep -E "error|model_unloaded" 
   (récupère la signature exacte des 3 timeouts du 25/04 et du model_unloaded 
   du 25/04 13:32 — tu dois pouvoir distinguer les 2 sur le terrain).
5. grep -rn "chat_completion_sync\|chat_completion_async" jarvis/ — liste 
   les call sites qui peuvent voir le nouveau RuntimeError "inference stuck". 
   Tu dois prouver qu'aucun ne dépend du comportement "retry après 1114s".
6. Identifie les 2 décisions de design à arbitrer EXPLICITEMENT (et propose-les 
   à Jean si doute) :
   - Trigger inference_stuck après attempt 0 (1 timeout, ~120s) ou attempt 1 
     (2 timeouts, ~242s) ? Recommandé : attempt >= 1 — laisse 1 retry pour 
     les long-prompts légitimes (760s observés sur 1 OK), abort dès le 2e 
     échec consécutif sur un modèle "ok". Compromis vs mur 1114s.
   - lm_state == "ok" suffit-il, ou faut-il aussi vérifier que le model_id 
     ciblé est dans la liste retournée par check_lm_studio ? Recommandé : 
     "ok" suffit pour ce SHIP — on traite check_lm_studio comme un binaire 
     "joignable + au moins un modèle". L'affinage modèle-précis appartient 
     à un futur SHIP.

Écris un rapport ~25 lignes listant :
- Le diagramme actuel (post v13-S2) : APITimeoutError → check_lm_studio() → 
  branche "no_model_loaded" raise OR sinon backoff + retry jusqu'à 3 fois.
- Le diagramme cible : APITimeoutError → check_lm_studio() → branche 
  "no_model_loaded" raise (inchangée) OR branche NEW "ok" + attempt >= 1 → 
  trace error_type="inference_stuck" + raise RuntimeError immédiat OR sinon 
  backoff + retry (inchangé).
- Les call sites identifiés (server.py /chat, nightly_learner._llm_extract, 
  daily_brief_generator, status_generator, evaluate-challenge) avec verdict 
  "regression-safe oui/non/à investiguer" pour chacun.
- Les 2 arbitrages design + ta proposition.

ATTENDS validation explicite avant de coder.

Objectif : Sur un run où LM Studio renvoie "ok" mais l'inférence est stuck 
(VRAM saturée ou run-away), abort en ≤ 250s avec trace lisible et 
/health "degraded" — au lieu de brûler 1114s en silence.

Fichiers concernés :
- jarvis/llm_client.py (modification ciblée du except APITimeoutError dans 
  _sync_call, ajout d'une branche "ok" + attempt >= 1)
- jarvis/nightly_learner.py (ajouter une 3e branche aux 2 catch existants 
  pour state["last_result"] = "lm_studio_inference_stuck", early return)
- jarvis/server.py L157 (ajouter "lm_studio_inference_stuck" à la liste 
  des last_result qui passent /health en degraded)

Étapes (après validation) :

1. Dans jarvis/llm_client.py, dans le bloc except APITimeoutError | 
   APIConnectionError de _sync_call, après la branche existante 
   `if lm_state == "no_model_loaded"` :
   ```python
   elif lm_state == "ok" and attempt >= 1:
       _write_trace({
           "ts": datetime.now(timezone.utc).isoformat(),
           "model": model_used,
           "chars_in": chars_in,
           "latency_ms": round((time.time() - start_time) * 1000),
           "attempts": attempt + 1,
           "status": "error",
           "error_type": "inference_stuck",
       })
       raise RuntimeError(
           f"LM Studio inference stuck on {model_used} (attempt {attempt + 1}/{MAX_RETRIES + 1})"
       ) from e
   ```
   Note : `attempt >= 1` veut dire qu'on tolère 1 retry — le premier timeout 
   peut être un long-prompt légitime ou flaky GPU. Le DEUXIÈME timeout sur 
   "ok" est le signal stuck.

2. Dans jarvis/nightly_learner.py, copie-colle exactement la branche 
   "lm_studio_unloaded_midrun" déjà présente aux L460-465 et L497-502, 
   ajoute juste après :
   ```python
   except RuntimeError as e:
       if "inference stuck" in str(e).lower():
           state["last_result"] = "lm_studio_inference_stuck"
           _save_state(state)
           return {"status": "error", "reason": "lm_studio_inference_stuck"}
       raise
   ```
   (ou intègre-le dans le except RuntimeError existant si la structure le 
   permet — ne PAS casser le handler unload existant).

3. Dans jarvis/server.py L157 :
   ```python
   if nightly.get("last_result") in ("empty_extraction", "extraction_model_unavailable", "lm_studio_unloaded_midrun", "lm_studio_inference_stuck"):
       result["status"] = "degraded"
   ```

4. Test reproductible (sans casser la prod) :
   - Démarre LM Studio + 9B chargé. Lance un long chat parallèle (8k tokens 
     d'historique). Pendant ce chat, lance manuellement :
     ```bash
     python -c "from jarvis.llm_client import chat_completion_sync; \
       out, t = chat_completion_sync([{'role':'user','content':'ping ' * 200}], 
       max_tokens=100); print('OK', t)"
     ```
   - Si VRAM est saturée, l'inférence va timeout — devrait afficher le 
     RuntimeError "stuck" en ≤ 250s (au lieu de 1114s).
   - grep -c "inference_stuck" jarvis_data/llm_traces.jsonl → ≥ 1
   - Si reproduction VRAM trop pénible, alternative : mock APITimeoutError 
     dans test_jarvis.py, vérifier que le code-path raise bien.

Contraintes :
- Ne supprime PAS la branche "no_model_loaded" existante — elle est 
  orthogonale, elle reste utile.
- Ne change PAS la signature publique de chat_completion_sync / 
  chat_completion_async.
- Ne réduit PAS le timeout client (toujours 120s) — sinon on génère des 
  faux positifs sur les long-prompts légitimes.
- Ne touche pas au préflight nightly_learner — il complète ce SHIP au 
  démarrage, pas à l'inverse.
- Ne touche pas à _route_llm dans server.py (HTTPException 504 reste 
  correct pour /chat).
- Pas de nouvelle dépendance, pas de variable d'env nouvelle.
- check_lm_studio doit garder son timeout court (2s) — sinon on rajoute 
  du temps mort sur le retry.

Validation :
- python -m py_compile jarvis/llm_client.py jarvis/nightly_learner.py jarvis/server.py
  → exit 0
- grep -c "inference_stuck" jarvis/llm_client.py → ≥ 2 (1 dans le trace, 
  1 dans le RuntimeError message)
- grep -c "lm_studio_inference_stuck" jarvis/nightly_learner.py jarvis/server.py 
  → ≥ 2 (1 dans nightly, 1 dans server /health)
- Test pratique (cf étape 4) : trace contient `inference_stuck` après 
  ≤ 250s OU mock unit test passe.
- python -m py_compile $(grep -lE "chat_completion" jarvis/*.py) → tous OK 
  (pas de régression syntaxe sur les call sites).

Ne fais PAS :
- N'introduis pas de circuit breaker stateful inter-runs (pour 1 cas/jour, 
  pas de valeur).
- N'ajoute pas de notification email/Slack/push (hors scope).
- Ne refactore pas _sync_call au-delà du bloc except cité.
- Ne supprime pas la branche `if attempt < MAX_RETRIES` existante en aval 
  (les APIConnectionError doivent toujours retry — réseau flake ≠ inference 
  stuck).
- Ne touche pas au cockpit (panel-jarvis.jsx, etc.).
- Ne push pas.

Quand c'est fait : montre-moi le diff complet AVANT git add + une trace 
JSONL d'un test réel ou mock (1 ping OK + 1 ping après saturation/mock). 
git commit avec message "feat(jarvis): detect LM Studio inference_stuck 
(VRAM-saturated), abort fast with state.degraded".
PAS de push.
```

#### Rechallenging — S1
- Red team : "Tu détectes via une heuristique fragile (lm_state == 'ok' + attempt >= 1). Et si l'utilisateur a juste un long prompt légitime qui timeout 2 fois ?" Réfutation : le seuil `attempt >= 1` (donc 2 timeouts consécutifs ~ 242s) attrape exactement le cas pathologique qu'on a observé (3 timeouts × 1114s) sans punir un long-prompt légitime qui aurait peut-être 1 timeout puis succès au retry. Compromis cadré.
- Contre-exemple : si LM Studio hostait à un moment 2 modèles dont un seulement chargé, la branche `"ok"` ne distinguerait pas "le modèle ciblé est encore là". Reuté : check_lm_studio actuel ne fait pas cette distinction non plus, et v13-S2 a accepté la même limite — orthogonal au scope. Listé en arbitrage Phase 0.
- Coût d'opportunité : 1.5-2h vs (a) Mem0g graph (M, hors scope), (b) hybrid search BM25 (M, hors scope), (c) wiki TypeError fix (XS, hors North Star — listé en P33). S1 reste meilleur usage : c'est le seul angle de la North Star qu'on n'a pas encore couvert.
- Futur soi : "J'ai sur-investi sur 1 cas qui ne se reproduira pas." Probabilité réelle : la VRAM saturée a brûlé 55 min sur 4 jours en avril (13/04, 14/04, 24/04, 25/04). Pattern récurrent depuis le passage au 9B le 25/04 — pari raisonnable que ça arrive ≥ 1x/semaine.
- Cohérence historique : v13-S2 (`f82b959`) a couvert orthogonalement le scénario unload. Ce SHIP étend, ne contredit pas. RAS.
- **Verdict final : MAINTENU**

### S2 — Lint CI bloquant : `KNOWN_SECTIONS` doit refléter `cockpit/app.jsx`

**Pourquoi maintenant** : sert directement la North Star. v13-S1 a fait la resync manuelle le 25/04 à 15:25. À 18:45 le même jour, le panel `veille-outils` a été ajouté (commit `4d742f7`). La liste est déjà obsolète sans qu'aucun audit n'ait tourné. Le pattern `manuel → drift → resync manuel` est déjà connu côté `docs/specs/` (4 lints CI bloquants Phase 7). Appliquer la même grammaire ici ferme la boucle pour de bon.
**Diagnostic** : `jarvis/scripts/extract_signals.py:31-37` (set hardcodé) vs `cockpit/app.jsx:425-475` (chaîne `else if (activePanel === "...")`). Pattern grep simple : `grep -oE 'activePanel\s*===\s*"[a-z_-]+"' cockpit/app.jsx | sort -u` retourne 28 entrées dont **`veille-outils` (manquant dans KNOWN_SECTIONS)**.
**Définition de "fait"** : (1) `scripts/lint_known_sections.py` créé et exit 1 sur l'état actuel (signale `veille-outils` manquant). (2) `KNOWN_SECTIONS` mis à jour pour inclure `veille-outils`. (3) Le lint exit 0. (4) Workflow CI `.github/workflows/lint-known-sections.yml` créé, déclenché sur changes à `cockpit/app.jsx` ou `extract_signals.py`. (5) Sur prochain ajout de panel sans update de KNOWN_SECTIONS, la PR est bloquée.
**Effort** : XS (~30 min)

#### Prompt Claude Code — S2

```
Contexte projet : KNOWN_SECTIONS dans jarvis/scripts/extract_signals.py est 
maintenu manuellement. v13-S1 a fait une resync le 25/04 15:25, et 3h plus 
tard (18:45) le panel "veille-outils" a été ajouté à cockpit/app.jsx — la 
liste a dérivé immédiatement. Le projet a déjà 4 lints CI Phase 7 qui forcent 
la cohérence specs ↔ code. Étendre cette grammaire à KNOWN_SECTIONS = 
fermer la boucle de drift définitivement.

Phase 0 — Reconnaissance (OBLIGATOIRE avant toute action)

1. Vérifie l'état actuel :
   - grep -oE 'activePanel\s*===\s*"[a-z_-]+"' cockpit/app.jsx | sed -E 's/.*"([^"]+)".*/\1/' | sort -u
     → tu dois voir 28 entrées (anime, brief, challenges, claude, gaming, 
     gaming_news, history, ideas, jarvis, jarvis-lab, jobs, music, news, 
     opps, perf, profile, radar, recos, review, search, signals, sport, 
     stacks, top, updates, **veille-outils**, week, wiki).
   - grep -A8 "^KNOWN_SECTIONS" jarvis/scripts/extract_signals.py
     → tu dois voir 27 entrées (les 28 ci-dessus MOINS "veille-outils").
2. Lis scripts/lint_specs_produit.py (~50 premières lignes). C'est ton 
   template de style : Python pur, pas de dépendances, exit 1 + message 
   clair, executable localement.
3. Lis .github/workflows/lint-specs.yml. C'est ton template de workflow : 
   triggers `push` + `pull_request` sur paths spécifiques, runs-on ubuntu, 
   setup-python 3.11, run script.
4. Identifie 1 décision de design :
   - Faut-il une "allowlist" de panels admis dans KNOWN_SECTIONS mais absents 
     d'app.jsx (par ex. sections logiques jamais exposées comme "tft" ou 
     "rte" historiques) ? Recommandé : non — toute entrée KNOWN_SECTIONS qui 
     n'a pas de match `activePanel === "X"` dans app.jsx est un mort-vivant 
     à supprimer. Si Jean a besoin d'une exception future, il l'ajoutera 
     comme constante explicite dans le lint avec commentaire.

Écris un rapport ~15 lignes :
- Diff prévu sur KNOWN_SECTIONS (1 ligne ajoutée : "veille-outils").
- Liste des 28 sections que le lint contrôle.
- Décision design + justification.
- Plan en 4 fichiers : scripts/lint_known_sections.py (créé), 
  jarvis/scripts/extract_signals.py (modifié — ajout veille-outils + bump 
  date commentaire), .github/workflows/lint-known-sections.yml (créé), 
  rien d'autre.

ATTENDS validation explicite.

Objectif : Toute future PR qui ajoute un `activePanel === "X"` dans 
cockpit/app.jsx doit aussi mettre à jour KNOWN_SECTIONS, sinon merge bloqué.

Fichiers concernés :
- scripts/lint_known_sections.py (création — ~50 lignes Python pur)
- jarvis/scripts/extract_signals.py (modification — ajouter "veille-outils" 
  à KNOWN_SECTIONS, bumper le commentaire)
- .github/workflows/lint-known-sections.yml (création — ~25 lignes)

Étapes (après validation) :

1. Créer scripts/lint_known_sections.py :
   ```python
   #!/usr/bin/env python3
   """
   Lint bloquant : KNOWN_SECTIONS dans jarvis/scripts/extract_signals.py
   doit refléter exactement les `activePanel === "X"` de cockpit/app.jsx.
   
   Source de vérité : cockpit/app.jsx (chaîne else-if du router).
   Régression cible : audit hebdo qui flagge des "Section inconnue" parce 
   qu'on a oublié de resync KNOWN_SECTIONS après ajout d'un panel.
   """
   from __future__ import annotations
   import re, sys
   from pathlib import Path
   
   ROOT = Path(__file__).resolve().parent.parent
   APP_JSX = ROOT / "cockpit" / "app.jsx"
   EXTRACT = ROOT / "jarvis" / "scripts" / "extract_signals.py"
   
   def discover_panels() -> set[str]:
       text = APP_JSX.read_text(encoding="utf-8")
       return set(re.findall(r'activePanel\s*===\s*"([a-z_-]+)"', text))
   
   def parse_known_sections() -> set[str]:
       text = EXTRACT.read_text(encoding="utf-8")
       m = re.search(r'KNOWN_SECTIONS\s*=\s*\{([^}]+)\}', text, re.DOTALL)
       if not m:
           print("[FAIL] KNOWN_SECTIONS introuvable dans extract_signals.py", 
                 file=sys.stderr)
           sys.exit(1)
       return set(re.findall(r'"([a-z_-]+)"', m.group(1)))
   
   def main() -> int:
       panels = discover_panels()
       known = parse_known_sections()
       if not panels:
           print("[FAIL] Aucun activePanel trouvé dans app.jsx — pattern cassé ?", 
                 file=sys.stderr)
           return 1
       missing = panels - known
       extra = known - panels
       if not missing and not extra:
           print(f"[OK] KNOWN_SECTIONS synchronisé ({len(panels)} panels).")
           return 0
       if missing:
           print(f"[FAIL] Panels présents dans app.jsx mais absents de KNOWN_SECTIONS :", 
                 file=sys.stderr)
           for p in sorted(missing): print(f"  + {p}", file=sys.stderr)
       if extra:
           print(f"[FAIL] Sections dans KNOWN_SECTIONS mais sans activePanel correspondant :", 
                 file=sys.stderr)
           for p in sorted(extra): print(f"  - {p}", file=sys.stderr)
       print("\nFix : éditer jarvis/scripts/extract_signals.py et resync.", 
             file=sys.stderr)
       return 1
   
   if __name__ == "__main__":
       sys.exit(main())
   ```

2. Mettre à jour jarvis/scripts/extract_signals.py :
   - Ajouter "veille-outils" à KNOWN_SECTIONS (ordre alphabétique, fin de la 
     ligne contenant "stacks, top, updates,")
   - Bumper le commentaire :
     # Source de vérité : cockpit/app.jsx, chaîne `else if (activePanel === "...")`.
     # Synchronisation forcée par scripts/lint_known_sections.py + CI 
     # .github/workflows/lint-known-sections.yml (bloquant).
     # Dernière MAJ : 2026-04-26 (ajout veille-outils + lint CI).

3. Créer .github/workflows/lint-known-sections.yml :
   ```yaml
   name: Lint KNOWN_SECTIONS sync
   
   on:
     push:
       paths:
         - 'cockpit/app.jsx'
         - 'jarvis/scripts/extract_signals.py'
         - 'scripts/lint_known_sections.py'
         - '.github/workflows/lint-known-sections.yml'
     pull_request:
       paths:
         - 'cockpit/app.jsx'
         - 'jarvis/scripts/extract_signals.py'
         - 'scripts/lint_known_sections.py'
         - '.github/workflows/lint-known-sections.yml'
   
   jobs:
     lint:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-python@v5
           with:
             python-version: '3.11'
         - name: Vérifier sync KNOWN_SECTIONS ↔ app.jsx (bloquant)
           run: python scripts/lint_known_sections.py
   ```

Contraintes :
- Le lint doit être Python pur (no deps), conforme au pattern de 
  scripts/lint_specs_produit.py.
- Le workflow doit reuser actions/checkout@v4 + setup-python@v5 (cohérence 
  avec lint-specs.yml).
- Ne refactore pas extract_signals.py au-delà de KNOWN_SECTIONS + commentaire.
- Ne touche pas aux 8 règles d'anomalies.
- N'ajoute pas une "allowlist" sans justification — KISS.

Validation :
- python scripts/lint_known_sections.py → exit 0 + message "[OK] 
  KNOWN_SECTIONS synchronisé (28 panels)."
- (Test négatif) Renomme temporairement "wiki" → "wiki2" dans extract_signals.py 
  → python scripts/lint_known_sections.py → exit 1 + erreurs claires. 
  Restaure ensuite.
- yamllint .github/workflows/lint-known-sections.yml (si dispo localement) 
  OR : python -c "import yaml; yaml.safe_load(open('.github/workflows/lint-known-sections.yml'))"
  → pas d'exception.

Ne fais PAS :
- N'utilise pas un parser AST jsx — la regex sur 1 pattern stable suffit.
- Ne fais PAS de runtime parsing dans extract_signals.py — c'est un lint 
  CI, pas une feature du script.
- N'ajoute pas une "allowlist" de sections autorisées — le lint doit 
  refléter la stricte égalité.
- Ne push pas.
- Ne touche pas à scripts/lint_specs_produit.py ni à lint-specs.yml.

Quand c'est fait : montre-moi le diff complet AVANT git add. 
git commit avec message "ci(audit): lint bloquant KNOWN_SECTIONS sync vs 
cockpit/app.jsx + ajout veille-outils".
PAS de push.
```

#### Rechallenging — S2
- Red team : "Tu ajoutes un workflow CI pour 1 cas de drift par mois — over-engineering." Réfutation : le pattern manuel a dérivé en 3h cette semaine, pas en 1 mois. Le workflow tourne en <5s sur PR concernée, 0 bruit sinon. ROI break-even à la 2e dérive prévenue.
- Contre-exemple : Jean ajoute un panel temporaire "test" et le lint bloque le merge. Réfutation : ajouter "test" à KNOWN_SECTIONS = 30s. Pas pire que les 4 lints Phase 7 qu'il a déjà acceptés. Et le lint produit un message d'erreur explicite + commande de fix.
- Coût d'opportunité : 30 min vs (a) wiki TypeError (XS, hors North Star), (b) Chunks: 0 fix dans status_generator (XS, hors North Star), (c) cloud fallback nightly (S, redondant avec S1 + v13-S2 — listé en P32). S2 sert directement la North Star et porte une dette structurelle (le diagnostic doit rester fidèle au cockpit qui évolue vite).
- Futur soi : "J'ai mis un lint CI sur un truc trivialement maintenable manuellement." Possible — mais le pattern v13-S1 (manuel) a dérivé en 3h. On a la preuve empirique que le manuel ne tient pas. ROI long terme : -1 ligne de bruit par audit qui aurait sinon flaggé "Section inconnue" parce qu'un panel a été ajouté sans resync.
- Cohérence historique : RAS — pas de PARK ou KILL antérieur sur ce sujet. Évolution naturelle de v13-S1 (manuel → CI-enforced).
- **Verdict final : MAINTENU**

## SHIP rétrogradés pendant le rechallenging

- **S3 (tentatif) — Cloud fallback dans `nightly_learner._llm_extract` après échec local** → rétrogradé en **PARK P32**.
  - Rétrogradation déclenchée par : red team fort ("redondant avec S1 + v13-S2"). Une fois `model_unloaded_midrun` ET `inference_stuck` détectés et abortés vite, le crash nightly devient "1 chunk perdu" au lieu de "55 min brûlées". Le fallback cloud résolvait le mauvais problème (dépendance LM Studio) au lieu du vrai (silent slow failure). Coût d'opportunité (2h) > valeur réelle après S1 shippé.
  - Condition de réveil : si après 2 semaines avec S1 mergé, on observe ≥ 3 nuits consécutives sans extraction (zéro fait extrait) malgré abort propre.
- **S4 (tentatif) — Fix wiki TypeError sur `e.word_count.toLocaleString` (108 occurrences)** → rétrogradé en **PARK P33**.
  - Rétrogradation déclenchée par : red team "C'est un bug UX bruyant, pas une silent failure — hors North Star." Valide : le North Star explicite "fini les défaillances silencieuses" — et le wiki TypeError est tout sauf silencieux (il s'affiche, il est compté dans télémétrie). Bon SHIP, mauvaise semaine.
  - Condition de réveil : prochaine fenêtre où North Star est UX-focused, OU si > 200 occurrences/semaine, OU si Jean rapporte une frustration explicite.

## KILL list

### K1 — Hygiène : déplacer les 3 prompts v13 (S1, S2, K1) `pending/` → `done/` + actualiser `prompts/README.md`

**Raison** : Les 3 prompts v13 sont shippés (commits `e1b27bc`, `f82b959`, `dca2849`) mais traînent encore dans `jarvis/upgrades/prompts/pending/`. `prompts/README.md` mentionne aussi un v10-S1 qui n'est plus dans pending (déjà déplacé par v13-K1 dans done/). État incohérent : le README ne reflète plus la réalité du dossier. Bruit cognitif à chaque ouverture du dossier prompts.
**Risque de regret** : Faible — les fichiers continuent d'exister dans `done/`, ils sont juste classés. Le README est un état instantané "à faire", pas un historique.
**Rechallenging court** :
- Test 1 (red team) : "Tu fais du chore qui aurait été fait au moment de shipper." Réfutation : c'est exactement le rôle d'une routine d'audit — rattraper ce qui n'a pas été classé en flow. Coût 5 min.
- Test 4 (futur soi) : "J'ai cassé une recherche future." Aucun risque — le dossier `done/` est l'historique normal.

**Prompt Claude Code** :

```
Phase 0 — Vérification :
1. ls jarvis/upgrades/prompts/pending/ → tu dois voir exactement 3 fichiers : 
   2026-04-25-K1-kill-stale-log-and-pending.md
   2026-04-25-S1-resync-known-sections.md
   2026-04-25-S2-lm-studio-unload-detection.md
2. git log --oneline --grep="resync KNOWN_SECTIONS" 
   → doit trouver e1b27bc.
3. git log --oneline --grep="model unload" 
   → doit trouver f82b959.
4. git log --oneline --grep="kill stale nightly_learner" 
   → doit trouver dca2849.
5. ls jarvis/upgrades/prompts/done/ | grep "2026-04-25" 
   → vérifier qu'aucun homonyme n'existe avant déplacement (sinon renommer 
   les nouveaux en -v2.md).

Si OK :
6. git mv jarvis/upgrades/prompts/pending/2026-04-25-K1-kill-stale-log-and-pending.md \
        jarvis/upgrades/prompts/done/2026-04-25-K1-kill-stale-log-and-pending.md
7. git mv jarvis/upgrades/prompts/pending/2026-04-25-S1-resync-known-sections.md \
        jarvis/upgrades/prompts/done/2026-04-25-S1-resync-known-sections.md
8. git mv jarvis/upgrades/prompts/pending/2026-04-25-S2-lm-studio-unload-detection.md \
        jarvis/upgrades/prompts/done/2026-04-25-S2-lm-studio-unload-detection.md

9. Réécris jarvis/upgrades/prompts/README.md pour refléter le nouvel 
   état (les 3 prompts du 2026-04-26 — S1 inference_stuck + S2 lint 
   KNOWN_SECTIONS + K1 hygiène — créés par l'audit du jour) :
   ```markdown
   # Prompts Claude Code en attente
   
   | Fichier | Titre | Effort | Date audit |
   |---------|-------|--------|------------|
   | [v14-S1](pending/2026-04-26-S1-inference-stuck-detection.md) | Détection LM Studio inference_stuck | S | 2026-04-26 |
   | [v14-S2](pending/2026-04-26-S2-lint-known-sections.md) | Lint CI bloquant KNOWN_SECTIONS sync | XS | 2026-04-26 |
   | [v14-K1](pending/2026-04-26-K1-hygiene-v13-prompts.md) | Hygiène : v13 prompts pending → done | XS | 2026-04-26 |
   
   **Workflow** : copier-coller le contenu d'un fichier dans Claude Code, 
   exécuter, revoir le diff, commit, puis déplacer le fichier vers `done/`. 
   Pour cette itération, exécuter K1 EN PREMIER (5 min, range les v13) 
   pour ne pas mélanger les états.
   ```

git commit avec message "chore(jarvis): hygiène — v13 prompts pending → done 
+ refresh prompts README".
PAS de push.
```

## PARK

| Idée | Condition de réveil |
|------|---------------------|
| P27 — Bump `project_status.yaml` avec Phase 7 méta-doc | Quand les 4 lints CI Phase 7 tournent 7j sans warning ni intervention manuelle |
| P28 — Hybrid search BM25 + vector + RRF | Maintenant que vectors=883 (≥500) ET 1 SHIP M de capacité disponible la semaine — donc semaine prochaine si pas d'incident urgent |
| P29 — Mem0g graph extension | Quand P28 shippé ET ≥ 3 cas de contradiction de faits observés dans `profile_facts` en 1 semaine |
| P30 — `signals.md` du 25/04 puis 26/04 manquants — investiguer la tâche planifiée Windows | **Condition remplie** (2e jour consécutif manqué) — à reprendre dès qu'un SHIP de capacité libre apparaît OU qu'un audit consécutif rate (3e fois) |
| P32 — Cloud fallback dans `nightly_learner._llm_extract` après échec local | Si après 2 semaines avec S1 mergé, ≥ 3 nuits consécutives sans extraction malgré abort propre |
| P33 — Fix wiki TypeError sur `e.word_count.toLocaleString` (108 occurrences) | Prochaine fenêtre où North Star est UX-focused, OU > 200 occurrences/semaine, OU Jean rapporte une frustration explicite |
| P34 — Fix `Chunks: 0` bug dans `status_generator.get_chunks_count()` (Content-Range mal lu) | Quand le snapshot Jarvis Lab est consulté ≥ 3x/semaine (signal de réutilisation) OU si une feature dépend de cette métrique |

## Edge — Ce qu'on pique cette semaine

**Rien à voler cette semaine.** La North Star est défensive (silent failures côté local) — le travail est interne, focused sur la fiabilité de l'inférence locale et la rigueur du diagnostic. Pas d'analyse externe pertinente sur ce périmètre exact en moins de 6 mois publiés. Repérage Mem0g + hybrid search RRF déjà capturé en P28/P29 depuis 25/04 — les exécuter quand une fenêtre M se libère.

## Tendance

Jarvis va **mieux qu'il y a 7 jours**, mais avec une nuance. v13 a fermé deux trous (KNOWN_SECTIONS resync, model_unloaded_midrun) et le refactor Qwen3.5 9B Instruct a éliminé le slot contention nightly ↔ chat. La passe UX shell + Morning Card + Veille outils + Claude Ecosystem sont des fondations solides côté produit. Mais le passage au 9B unique a aussi exposé un nouveau mode d'échec (VRAM saturée → inference_stuck) qui a brûlé 55 min le 25/04 — la même empreinte que les pannes pré-v13 sous une autre cause. La North Star de cette semaine ferme cette faille, et ajoute un garde-fou structurel (S2 lint) pour empêcher la dérive du diagnostic. Les 30 PARK accumulés (P1-P34) signalent qu'il faudra bientôt élaguer (consolider ou tuer franchement plutôt que reporter).

## Méta-tendance (mensuelle, à remplir le 1er audit du mois)

*Pas premier audit du mois — section non remplie cette semaine. Prochain check : audit du 2026-05-01 ou suivant.*

---

## Dernière MAJ

- **2026-04-26** : audit v14. 2 SHIP (inference_stuck detection + lint CI KNOWN_SECTIONS), 1 KILL (hygiène v13 prompts), 2 SHIP rétrogradés (cloud fallback nightly → P32, wiki TypeError → P33), 1 PARK ajouté (P34 Chunks: 0 status_generator). Note : signals.md 26/04 absent (2e jour consécutif manqué — P30 condition remplie mais pas adressé ce tour-ci).
